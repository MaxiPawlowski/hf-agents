# Claude Adapter

Claude-specific material belongs here.

The framework itself lives at the repo root:

- `agents/`
- `subagents/`
- `skills/` for the active reusable procedures only

`settings.local.json` is intentionally local-only and ignored by git.
