# Codex Adapter Notes

Canonical framework assets live in:

- `agents/`
- `subagents/`
- `skills/`
- `plans/`

Use the root assets as the source of truth. Do not recreate parallel copies inside `.codex/`.
