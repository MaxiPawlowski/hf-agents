# Codex Adapter

Codex should treat the repo root as canonical:

- `agents/` for planner and builder orchestrators
- `subagents/` for worker roles
- `skills/` for the small active workflow set used by those roles

This folder exists only for Codex-facing adapter notes.
