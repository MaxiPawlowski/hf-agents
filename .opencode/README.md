# OpenCode Adapter

This folder is no longer the source of truth for the framework.

Canonical assets live in the repo root:

- `agents/`
- `subagents/`
- `skills/`

`registry.json` exists only to map OpenCode-facing asset IDs back to the active root files.
