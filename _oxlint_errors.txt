
  ! eslint-plugin-promise(catch-or-return): eslint-plugin-promise(catch-or-return): Expected catch or return
    ,-[src/runtime/utils.ts:13:7]
 12 |         timer = setTimeout(() => {
 13 | ,->       Promise.resolve()
 14 | |           .then(onDeadline)
 15 | `->         .then(resolve, reject);
 16 |         }, ms);
    `----
  help: Return the promise or chain a catch()

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/code-chunker.test.ts:56:12]
 55 |     expect(chunks).toHaveLength(1);
 56 |     expect(chunks[0]!.text).toContain("Explains the helper.");
    :            ^^^^^^^^^^
 57 |     expect(chunks[0]!.text.startsWith("/**")).toBe(true);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/code-chunker.test.ts:57:12]
 56 |     expect(chunks[0]!.text).toContain("Explains the helper.");
 57 |     expect(chunks[0]!.text.startsWith("/**")).toBe(true);
    :            ^^^^^^^^^^
 58 |   });
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/code-chunker.test.ts:81:12]
 80 |     expect(chunks).toHaveLength(2);
 81 |     expect(chunks[1]!.metadata.sectionTitle).toBe("ID");
    :            ^^^^^^^^^^
 82 |     expect(chunks[1]!.text).toContain("export type ID=1;");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/code-chunker.test.ts:82:12]
 81 |     expect(chunks[1]!.metadata.sectionTitle).toBe("ID");
 82 |     expect(chunks[1]!.text).toContain("export type ID=1;");
    :            ^^^^^^^^^^
 83 |     expect(chunks[1]!.text).toContain("export interface Profile");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/code-chunker.test.ts:83:12]
 82 |     expect(chunks[1]!.text).toContain("export type ID=1;");
 83 |     expect(chunks[1]!.text).toContain("export interface Profile");
    :            ^^^^^^^^^^
 84 |   });
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/code-chunker.test.ts:99:12]
  98 |     expect(chunksA.map((chunk) => chunk.id)).toEqual(chunksB.map((chunk) => chunk.id));
  99 |     expect(chunksA[0]!.id).toBe("src/paths.ts#imports");
     :            ^^^^^^^^^^^
 100 |     expect(chunksA[1]!.id).toBe("src/paths.ts#buildPath");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/code-chunker.test.ts:100:12]
  99 |     expect(chunksA[0]!.id).toBe("src/paths.ts#imports");
 100 |     expect(chunksA[1]!.id).toBe("src/paths.ts#buildPath");
     :            ^^^^^^^^^^^
 101 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:10:12]
  9 |   for (let i = 0; i < a.length; i++) {
 10 |     dot += a[i]! * b[i]!;
    :            ^^^^^
 11 |     normA += a[i]! * a[i]!;
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:10:20]
  9 |   for (let i = 0; i < a.length; i++) {
 10 |     dot += a[i]! * b[i]!;
    :                    ^^^^^
 11 |     normA += a[i]! * a[i]!;
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:11:14]
 10 |     dot += a[i]! * b[i]!;
 11 |     normA += a[i]! * a[i]!;
    :              ^^^^^
 12 |     normB += b[i]! * b[i]!;
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:11:22]
 10 |     dot += a[i]! * b[i]!;
 11 |     normA += a[i]! * a[i]!;
    :                      ^^^^^
 12 |     normB += b[i]! * b[i]!;
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:12:14]
 11 |     normA += a[i]! * a[i]!;
 12 |     normB += b[i]! * b[i]!;
    :              ^^^^^
 13 |   }
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:12:22]
 11 |     normA += a[i]! * a[i]!;
 12 |     normB += b[i]! * b[i]!;
    :                      ^^^^^
 13 |   }
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:88:26]
 87 |     for (let i = 0; i < texts.length; i++) {
 88 |       const sim = cosine(batchResults[i]!, individual[i]!);
    :                          ^^^^^^^^^^^^^^^^
 89 |       expect(sim).toBeGreaterThan(0.99);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-embeddings.test.ts:88:44]
 87 |     for (let i = 0; i < texts.length; i++) {
 88 |       const sim = cosine(batchResults[i]!, individual[i]!);
    :                                            ^^^^^^^^^^^^^^
 89 |       expect(sim).toBeGreaterThan(0.99);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/e2e/vault-claude-e2e.test.ts:163:10]
 162 |   expect(managedSidecar, "Expected Claude managed-plan runtime sidecar for plan slug 'test'.").toBeDefined();
 163 |   return managedSidecar!;
     :          ^^^^^^^^^^^^^^^
 164 | }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:32:12]
 31 |     expect(chunks.length).toBe(3);
 32 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Context");
    :            ^^^^^^^^^^
 33 |     expect(chunks[1]!.metadata.sectionTitle).toBe("Decisions");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:33:12]
 32 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Context");
 33 |     expect(chunks[1]!.metadata.sectionTitle).toBe("Decisions");
    :            ^^^^^^^^^^
 34 |     expect(chunks[2]!.metadata.sectionTitle).toBe("References");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:34:12]
 33 |     expect(chunks[1]!.metadata.sectionTitle).toBe("Decisions");
 34 |     expect(chunks[2]!.metadata.sectionTitle).toBe("References");
    :            ^^^^^^^^^^
 35 | 
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:37:12]
 36 |     // Each chunk starts with its header
 37 |     expect(chunks[0]!.text).toContain("## Context");
    :            ^^^^^^^^^^
 38 |     expect(chunks[1]!.text).toContain("## Decisions");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:38:12]
 37 |     expect(chunks[0]!.text).toContain("## Context");
 38 |     expect(chunks[1]!.text).toContain("## Decisions");
    :            ^^^^^^^^^^
 39 |     expect(chunks[2]!.text).toContain("## References");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:39:12]
 38 |     expect(chunks[1]!.text).toContain("## Decisions");
 39 |     expect(chunks[2]!.text).toContain("## References");
    :            ^^^^^^^^^^
 40 | 
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:42:12]
 41 |     // Body text is included
 42 |     expect(chunks[0]!.text).toContain("Brief context about the plan");
    :            ^^^^^^^^^^
 43 |     expect(chunks[1]!.text).toContain("We decided to use TypeScript");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:43:12]
 42 |     expect(chunks[0]!.text).toContain("Brief context about the plan");
 43 |     expect(chunks[1]!.text).toContain("We decided to use TypeScript");
    :            ^^^^^^^^^^
 44 |   });
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:61:12]
 60 |     expect(chunks).toHaveLength(1);
 61 |     expect(chunks[0]!.text).toBe(content);
    :            ^^^^^^^^^^
 62 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Test Notes"); // falls back to doc title
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:62:12]
 61 |     expect(chunks[0]!.text).toBe(content);
 62 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Test Notes"); // falls back to doc title
    :            ^^^^^^^^^^
 63 |   });
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:80:12]
 79 |     expect(chunks.length).toBe(3);
 80 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Decisions");
    :            ^^^^^^^^^^
 81 |     expect(chunks[1]!.metadata.sectionTitle).toBe("Decision 1: Use TypeScript");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:81:12]
 80 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Decisions");
 81 |     expect(chunks[1]!.metadata.sectionTitle).toBe("Decision 1: Use TypeScript");
    :            ^^^^^^^^^^
 82 |     expect(chunks[2]!.metadata.sectionTitle).toBe("Decision 2: Use Vitest");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/vault-chunker.test.ts:82:12]
 81 |     expect(chunks[1]!.metadata.sectionTitle).toBe("Decision 1: Use TypeScript");
 82 |     expect(chunks[2]!.metadata.sectionTitle).toBe("Decision 2: Use Vitest");
    :            ^^^^^^^^^^
 83 |   });
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:102:12]
 101 |     expect(chunks.length).toBe(2);
 102 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Overview");
     :            ^^^^^^^^^^
 103 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:105:12]
 104 |     // Merged chunk uses the short section's header but includes both bodies
 105 |     expect(chunks[1]!.text).toContain("Tiny.");
     :            ^^^^^^^^^^
 106 |     expect(chunks[1]!.text).toContain("## Details");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:106:12]
 105 |     expect(chunks[1]!.text).toContain("Tiny.");
 106 |     expect(chunks[1]!.text).toContain("## Details");
     :            ^^^^^^^^^^
 107 |     expect(chunks[1]!.text).toContain("plenty of detail");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:107:12]
 106 |     expect(chunks[1]!.text).toContain("## Details");
 107 |     expect(chunks[1]!.text).toContain("plenty of detail");
     :            ^^^^^^^^^^
 108 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:123:12]
 122 |     expect(chunks.length).toBe(1);
 123 |     expect(chunks[0]!.text).toContain("## Main");
     :            ^^^^^^^^^^
 124 |     expect(chunks[0]!.text).toContain("Fin.");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:124:12]
 123 |     expect(chunks[0]!.text).toContain("## Main");
 124 |     expect(chunks[0]!.text).toContain("Fin.");
     :            ^^^^^^^^^^
 125 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:134:12]
 133 | 
 134 |     expect(chunks1[0]!.id).toBe(chunks2[0]!.id);
     :            ^^^^^^^^^^^
 135 |     expect(chunks1[0]!.id).toBe("vault/plans/my-plan/notes.md#Context");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:134:33]
 133 | 
 134 |     expect(chunks1[0]!.id).toBe(chunks2[0]!.id);
     :                                 ^^^^^^^^^^^
 135 |     expect(chunks1[0]!.id).toBe("vault/plans/my-plan/notes.md#Context");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:135:12]
 134 |     expect(chunks1[0]!.id).toBe(chunks2[0]!.id);
 135 |     expect(chunks1[0]!.id).toBe("vault/plans/my-plan/notes.md#Context");
     :            ^^^^^^^^^^^
 136 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:145:19]
 144 |     expect(chunks).toHaveLength(1);
 145 |     const chunk = chunks[0]!;
     :                   ^^^^^^^^^^
 146 |     expect(chunk.metadata.sourcePath).toBe("vault/shared/conventions.md");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:156:12]
 155 |     const chunks = chunkVaultDocument(d);
 156 |     expect(chunks[0]!.id).toBe("vault/plans/test/notes.md#Test");
     :            ^^^^^^^^^^
 157 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:172:12]
 171 |     // Preamble chunk uses document title as section title
 172 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Test Notes");
     :            ^^^^^^^^^^
 173 |     expect(chunks[0]!.text).toContain("preamble text");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:173:12]
 172 |     expect(chunks[0]!.metadata.sectionTitle).toBe("Test Notes");
 173 |     expect(chunks[0]!.text).toContain("preamble text");
     :            ^^^^^^^^^^
 174 |     expect(chunks[1]!.metadata.sectionTitle).toBe("First Section");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:174:12]
 173 |     expect(chunks[0]!.text).toContain("preamble text");
 174 |     expect(chunks[1]!.metadata.sectionTitle).toBe("First Section");
     :            ^^^^^^^^^^
 175 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:190:12]
 189 | 
 190 |     expect(chunks[0]!.text).toContain("**bold**");
     :            ^^^^^^^^^^
 191 |     expect(chunks[0]!.text).toContain("```ts");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/vault-chunker.test.ts:191:12]
 190 |     expect(chunks[0]!.text).toContain("**bold**");
 191 |     expect(chunks[0]!.text).toContain("```ts");
     :            ^^^^^^^^^^
 192 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/e2e/runtime-opencode-e2e.test.ts:50:26]
 49 | 
 50 |     const result = await hooks["session.created"]!({ sessionID: SESSION_ID });
    :                          ^^^^^^^^^^^^^^^^^^^^^^^^^
 51 | 
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/e2e/runtime-opencode-e2e.test.ts:71:7]
 70 |     await expect(
 71 |       hooks["tool.execute.before"]!({ command: "rm -rf /" })
    :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 72 |     ).rejects.toThrow("guardrail blocked a destructive command");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/e2e/runtime-opencode-e2e.test.ts:83:11]
 82 |     // context.session.id is used as the fallback sessionId since info has no sessionID.
 83 |     await hooks["message.updated"]!({
    :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 84 |       info: {
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/e2e/runtime-opencode-e2e.test.ts:92:31]
 91 |     // The interrupt flag is now set. session.idle should return allow_stop immediately.
 92 |     const firstResult = await hooks["session.idle"]!({ sessionID: SESSION_ID });
    :                               ^^^^^^^^^^^^^^^^^^^^^^
 93 |     expect(firstResult).toBeDefined();
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/e2e/runtime-opencode-e2e.test.ts:101:11]
 100 |     // Re-set the hf-agent flag so the agent gate doesn't swallow the second call.
 101 |     await hooks["message.updated"]!({
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 102 |       info: { role: "assistant", agent: "hf-builder" }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/e2e/runtime-opencode-e2e.test.ts:107:32]
 106 |     // With no plan binding, the runtime is null → session.idle returns null.
 107 |     const secondResult = await hooks["session.idle"]!({ sessionID: SESSION_ID });
     :                                ^^^^^^^^^^^^^^^^^^^^^^
 108 |     if (secondResult !== null) {
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/e2e/runtime-opencode-e2e.test.ts:126:11]
 125 |     // Activate the hf-agent gate so session.idle runs runtime logic.
 126 |     await hooks["message.updated"]!({
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 127 |       info: { role: "assistant", agent: "hf-builder" }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/e2e/runtime-opencode-e2e.test.ts:132:26]
 131 |     const messageWithOutcome = buildTurnOutcomePayload("progress");
 132 |     const result = await hooks["session.idle"]!({ sessionID: SESSION_ID, message: messageWithOutcome });
     :                          ^^^^^^^^^^^^^^^^^^^^^^
 133 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/e2e/runtime-opencode-e2e.test.ts:226:11]
 225 |     // Call session.created to trigger hydration and decideNext() → refreshVaultIndex().
 226 |     await hooks["session.created"]!({ sessionID: WARM_SESSION_ID });
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 227 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[src/runtime/vault-embeddings.ts:44:89]
 43 |       });
 44 |       const ext = await Promise.race([loadPromise, timeout]).finally(() => clearTimeout(timer!));
    :                                                                                         ^^^^^^
 45 |       extractor = ext;
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/turn-outcome-trailer.test.ts:51:14]
 50 |       expect(issues).toHaveLength(1);
 51 |       expect(issues[0]!.path).toBe("$");
    :              ^^^^^^^^^^
 52 |       expect(issues[0]!.message).toContain("expected object");
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/turn-outcome-trailer.test.ts:52:14]
 51 |       expect(issues[0]!.path).toBe("$");
 52 |       expect(issues[0]!.message).toContain("expected object");
    :              ^^^^^^^^^^
 53 |     }
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/turn-outcome-trailer.test.ts:201:14]
 200 |     if (result.kind === "invalid") {
 201 |       expect(result.errors[0]!.message).toContain("empty");
     :              ^^^^^^^^^^^^^^^^^
 202 |     }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/turn-outcome-trailer.test.ts:210:14]
 209 |     if (result.kind === "invalid") {
 210 |       expect(result.errors[0]!.message).toContain("invalid JSON");
     :              ^^^^^^^^^^^^^^^^^
 211 |     }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/embedding-ipc.test.ts:45:39]
 44 |   for (let index = 0; index < expected.length; index++) {
 45 |     expect(actual[index]).toBeCloseTo(expected[index]!, 5);
    :                                       ^^^^^^^^^^^^^^^^
 46 |   }
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/embedding-ipc.test.ts:52:23]
 51 |   for (let index = 0; index < expected.length; index++) {
 52 |     expectVectorClose(actual[index]!, expected[index]!);
    :                       ^^^^^^^^^^^^^^
 53 |   }
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/embedding-ipc.test.ts:52:39]
 51 |   for (let index = 0; index < expected.length; index++) {
 52 |     expectVectorClose(actual[index]!, expected[index]!);
    :                                       ^^^^^^^^^^^^^^^^
 53 |   }
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[src/runtime/vault-chunker.ts:68:21]
 67 |   for (let i = 0; i < input.length; i++) {
 68 |     const section = input[i]!;
    :                     ^^^^^^^^^
 69 | 
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[src/runtime/vault-chunker.ts:81:22]
 80 |         // Last section is short — merge backward into the previous merged section.
 81 |         const prev = merged[merged.length - 1]!;
    :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^
 82 |         prev.bodyLines.push(
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/vault-chunker.ts:158:17]
 157 |           id: `${makeChunkId(filePath, title)}:${i}`,
 158 |           text: parts[i]!,
     :                 ^^^^^^^^^
 159 |           metadata: { sourcePath: filePath, sectionTitle: title, documentTitle },
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:116:12]
 115 |     expect(result).not.toBeNull();
 116 |     expect(result!.version).toBe(1);
     :            ^^^^^^^
 117 |     expect(result!.planSlug).toBe(plan.slug);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:117:12]
 116 |     expect(result!.version).toBe(1);
 117 |     expect(result!.planSlug).toBe(plan.slug);
     :            ^^^^^^^
 118 |     expect(result!.loopState).toBe("idle");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:118:12]
 117 |     expect(result!.planSlug).toBe(plan.slug);
 118 |     expect(result!.loopState).toBe("idle");
     :            ^^^^^^^
 119 |     expect(result!.counters.totalAttempts).toBe(0);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:119:12]
 118 |     expect(result!.loopState).toBe("idle");
 119 |     expect(result!.counters.totalAttempts).toBe(0);
     :            ^^^^^^^
 120 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:140:31]
 139 |     expect(lines).toHaveLength(1);
 140 |     const parsed = JSON.parse(lines[0]!) as RuntimeEvent;
     :                               ^^^^^^^^^
 141 |     expect(parsed.type).toBe("test.created");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:152:23]
 151 |     expect(lines).toHaveLength(3);
 152 |     expect(JSON.parse(lines[0]!).type).toBe("event.one");
     :                       ^^^^^^^^^
 153 |     expect(JSON.parse(lines[2]!).type).toBe("event.three");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:153:23]
 152 |     expect(JSON.parse(lines[0]!).type).toBe("event.one");
 153 |     expect(JSON.parse(lines[2]!).type).toBe("event.three");
     :                       ^^^^^^^^^
 154 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:226:12]
 225 |     expect(context.plan).toHaveLength(1);
 226 |     expect(context.plan[0]!.title).toBe("Plan context");
     :            ^^^^^^^^^^^^^^^^
 227 |     expect(context.plan[0]!.content).toContain("Some content.");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:227:12]
 226 |     expect(context.plan[0]!.title).toBe("Plan context");
 227 |     expect(context.plan[0]!.content).toContain("Some content.");
     :            ^^^^^^^^^^^^^^^^
 228 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:230:12]
 229 |     expect(context.shared).toHaveLength(1);
 230 |     expect(context.shared[0]!.title).toBe("Shared architecture");
     :            ^^^^^^^^^^^^^^^^^^
 231 |     expect(context.shared[0]!.content).toContain("Shared notes.");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/persistence.test.ts:231:12]
 230 |     expect(context.shared[0]!.title).toBe("Shared architecture");
 231 |     expect(context.shared[0]!.content).toContain("Shared notes.");
     :            ^^^^^^^^^^^^^^^^^^
 232 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[src/runtime/chunk-utils.ts:90:19]
 89 |   for (let i = 0; i < input.length; i++) {
 90 |     const chunk = input[i]!;
    :                   ^^^^^^^^^
 91 | 
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/chunk-utils.ts:102:24]
 101 | 
 102 |       const previous = merged[merged.length - 1]!;
     :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^
 103 |       previous.text = `${previous.text}${separator}${chunk.text}`;
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/plan-doc.test.ts:205:23]
 204 |     expect(plan.milestones).toHaveLength(1);
 205 |     const milestone = plan.milestones[0]!;
     :                       ^^^^^^^^^^^^^^^^^^^
 206 |     expect(milestone.context?.scope).toEqual(["src/auth", "src/middleware"]);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/plan-doc.test.ts:308:12]
 307 | 
 308 |     expect(plan.milestones[0]!.title).toBe("Build the feature");
     :            ^^^^^^^^^^^^^^^^^^^
 309 |     expect(plan.milestones[0]!.text).toBe("1. Build the feature");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/plan-doc.test.ts:309:12]
 308 |     expect(plan.milestones[0]!.title).toBe("Build the feature");
 309 |     expect(plan.milestones[0]!.text).toBe("1. Build the feature");
     :            ^^^^^^^^^^^^^^^^^^^
 310 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/plan-doc.test.ts:327:15]
 326 |     const plan = await parsePlan(planPath);
 327 |     const m = plan.milestones[0]!;
     :               ^^^^^^^^^^^^^^^^^^^
 328 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/plan-doc.test.ts:347:12]
 346 | 
 347 |     expect(plan.milestones[0]!.context).toBeUndefined();
     :            ^^^^^^^^^^^^^^^^^^^
 348 |     expect(plan.milestones[0]!.reviewPolicy).toBeUndefined();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/plan-doc.test.ts:348:12]
 347 |     expect(plan.milestones[0]!.context).toBeUndefined();
 348 |     expect(plan.milestones[0]!.reviewPolicy).toBeUndefined();
     :            ^^^^^^^^^^^^^^^^^^^
 349 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-store.test.ts:61:14]
 60 |       expect(loaded).not.toBeNull();
 61 |       expect(loaded!.index).toEqual(index);
    :              ^^^^^^^
 62 |       expect(Array.from(loaded!.vectors.slice(0, 6))).toEqual(Array.from(vectors.slice(0, 6)));
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-store.test.ts:62:25]
 61 |       expect(loaded!.index).toEqual(index);
 62 |       expect(Array.from(loaded!.vectors.slice(0, 6))).toEqual(Array.from(vectors.slice(0, 6)));
    :                         ^^^^^^^
 63 |       expect(Array.from(loaded!.vectors.slice(384, 390))).toEqual(
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-store.test.ts:63:25]
 62 |       expect(Array.from(loaded!.vectors.slice(0, 6))).toEqual(Array.from(vectors.slice(0, 6)));
 63 |       expect(Array.from(loaded!.vectors.slice(384, 390))).toEqual(
    :                         ^^^^^^^
 64 |         Array.from(vectors.slice(384, 390)),
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-store.test.ts:76:12]
 75 |     expect(result.index.items).toHaveLength(1);
 76 |     expect(result.index.items[0]!.id).toBe("a");
    :            ^^^^^^^^^^^^^^^^^^^^^^
 77 |     expect(result.vectors).toHaveLength(384);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-store.test.ts:98:12]
 97 |     expect(updated.index.items).toHaveLength(1);
 98 |     expect(updated.index.items[0]!.metadata.kind).toBe("code");
    :            ^^^^^^^^^^^^^^^^^^^^^^^
 99 |     expect(updated.index.items[0]!.metadata.rank).toBe(2);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:99:12]
  98 |     expect(updated.index.items[0]!.metadata.kind).toBe("code");
  99 |     expect(updated.index.items[0]!.metadata.rank).toBe(2);
     :            ^^^^^^^^^^^^^^^^^^^^^^^
 100 |     expect(updated.vectors[0]).toBeCloseTo(0, 6);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:140:12]
 139 |     expect(results).toHaveLength(2);
 140 |     expect(results[0]!.id).toBe("high");
     :            ^^^^^^^^^^^
 141 |     expect(results[1]!.id).toBe("mid");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:141:12]
 140 |     expect(results[0]!.id).toBe("high");
 141 |     expect(results[1]!.id).toBe("mid");
     :            ^^^^^^^^^^^
 142 |     expect(results[0]!.score).toBeGreaterThan(results[1]!.score);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:142:12]
 141 |     expect(results[1]!.id).toBe("mid");
 142 |     expect(results[0]!.score).toBeGreaterThan(results[1]!.score);
     :            ^^^^^^^^^^^
 143 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:142:47]
 141 |     expect(results[1]!.id).toBe("mid");
 142 |     expect(results[0]!.score).toBeGreaterThan(results[1]!.score);
     :                                               ^^^^^^^^^^^
 143 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:350:12]
 349 |     expect(result.index.items).toHaveLength(2);
 350 |     expect(result.index.items[0]!.metadata.v).toBe(2); // updated
     :            ^^^^^^^^^^^^^^^^^^^^^^
 351 |     expect(result.index.items[1]!.id).toBe("b"); // new
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:351:12]
 350 |     expect(result.index.items[0]!.metadata.v).toBe(2); // updated
 351 |     expect(result.index.items[1]!.id).toBe("b"); // new
     :            ^^^^^^^^^^^^^^^^^^^^^^
 352 |     expect(result.vectors.length).toBe(384 * 2);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:408:14]
 407 |       // Must have exactly the items that the winning write serialised.
 408 |       expect(loaded!.index.items.length).toBeGreaterThan(0);
     :              ^^^^^^^
 409 |       expect(loaded!.vectors.length).toBe(384 * loaded!.index.items.length);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:409:14]
 408 |       expect(loaded!.index.items.length).toBeGreaterThan(0);
 409 |       expect(loaded!.vectors.length).toBe(384 * loaded!.index.items.length);
     :              ^^^^^^^
 410 |     } finally {
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-store.test.ts:409:49]
 408 |       expect(loaded!.index.items.length).toBeGreaterThan(0);
 409 |       expect(loaded!.vectors.length).toBe(384 * loaded!.index.items.length);
     :                                                 ^^^^^^^
 410 |     } finally {
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
    ,-[tests/unified-store.test.ts:18:18]
 17 | function makeVector(values: number[]): number[] {
 18 |   const vector = new Array<number>(384).fill(0);
    :                  ^^^^^^^^^^^^^^^^^^^^^^
 19 |   for (let i = 0; i < values.length; i++) {
    `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-index-pipeline.test.ts:87:24]
 86 |     expect(mockedEmbedBatch).toHaveBeenCalledTimes(1);
 87 |     expect(Object.keys(result!.index.fileHashes)).toHaveLength(2);
    :                        ^^^^^^^
 88 |     expect(result!.index.items.some((item) => item.metadata.kind === "vault")).toBe(true);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-index-pipeline.test.ts:88:12]
 87 |     expect(Object.keys(result!.index.fileHashes)).toHaveLength(2);
 88 |     expect(result!.index.items.some((item) => item.metadata.kind === "vault")).toBe(true);
    :            ^^^^^^^
 89 |     expect(result!.index.items.some((item) => item.metadata.kind === "code")).toBe(true);
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-index-pipeline.test.ts:89:12]
 88 |     expect(result!.index.items.some((item) => item.metadata.kind === "vault")).toBe(true);
 89 |     expect(result!.index.items.some((item) => item.metadata.kind === "code")).toBe(true);
    :            ^^^^^^^
 90 | 
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
    ,-[tests/unified-index-pipeline.test.ts:92:49]
 91 |     const persisted = await loadUnifiedIndex(tmpDir);
 92 |     expect(persisted?.index.fileHashes).toEqual(result!.index.fileHashes);
    :                                                 ^^^^^^^
 93 |   });
    `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:123:12]
 122 |     expect(second).not.toBeNull();
 123 |     expect(second!.index.fileHashes).toEqual(first!.index.fileHashes);
     :            ^^^^^^^
 124 |     expect(mockedEmbedBatch).not.toHaveBeenCalled();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:123:46]
 122 |     expect(second).not.toBeNull();
 123 |     expect(second!.index.fileHashes).toEqual(first!.index.fileHashes);
     :                                              ^^^^^^
 124 |     expect(mockedEmbedBatch).not.toHaveBeenCalled();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:159:7]
 158 |     expect(
 159 |       second!.index.items.find((item) => String(item.metadata.sourcePath).endsWith("b.ts"))?.text,
     :       ^^^^^^^
 160 |     ).toContain("three");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:162:7]
 161 |     expect(
 162 |       second!.index.items.find((item) => String(item.metadata.sourcePath).endsWith("a.ts"))?.text,
     :       ^^^^^^^
 163 |     ).toContain("one");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:193:7]
 192 |     expect(
 193 |       second!.index.items.some((item) => String(item.metadata.sourcePath).endsWith("b.ts")),
     :       ^^^^^^^
 194 |     ).toBe(false);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:195:24]
 194 |     ).toBe(false);
 195 |     expect(Object.keys(second!.index.fileHashes).some((filePath) => filePath.endsWith("b.ts"))).toBe(false);
     :                        ^^^^^^^
 196 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:238:24]
 237 |     expect(result).not.toBeNull();
 238 |     expect(Object.keys(result!.index.fileHashes)).toHaveLength(3);
     :                        ^^^^^^^
 239 |     expect(result!.index.items.filter((item) => item.metadata.kind === "vault")).toHaveLength(2);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:239:12]
 238 |     expect(Object.keys(result!.index.fileHashes)).toHaveLength(3);
 239 |     expect(result!.index.items.filter((item) => item.metadata.kind === "vault")).toHaveLength(2);
     :            ^^^^^^^
 240 |     expect(result!.index.items.filter((item) => item.metadata.kind === "code")).toHaveLength(1);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:240:12]
 239 |     expect(result!.index.items.filter((item) => item.metadata.kind === "vault")).toHaveLength(2);
 240 |     expect(result!.index.items.filter((item) => item.metadata.kind === "code")).toHaveLength(1);
     :            ^^^^^^^
 241 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:263:35]
 262 |     // fileHashes keys should be relative
 263 |     for (const key of Object.keys(result!.index.fileHashes)) {
     :                                   ^^^^^^^
 264 |       expect(path.isAbsolute(key)).toBe(false);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:269:24]
 268 |     // sourcePath metadata should be relative
 269 |     for (const item of result!.index.items) {
     :                        ^^^^^^^
 270 |       const sourcePath = String(item.metadata.sourcePath);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:293:12]
 292 |     // The single section should be split into multiple chunks
 293 |     expect(result!.index.items.length).toBeGreaterThan(1);
     :            ^^^^^^^
 294 |     // Each chunk's text should be <= 100 chars
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:295:24]
 294 |     // Each chunk's text should be <= 100 chars
 295 |     for (const item of result!.index.items) {
     :                        ^^^^^^^
 296 |       expect(item.text.length).toBeLessThanOrEqual(100);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:299:33]
 298 |     // All chunks should reference the same source
 299 |     const sourcePaths = new Set(result!.index.items.map((i) => String(i.metadata.sourcePath)));
     :                                 ^^^^^^^
 300 |     expect(sourcePaths.size).toBe(1);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:322:23]
 321 |     expect(result).not.toBeNull();
 322 |     const codeItems = result!.index.items.filter((item) => item.metadata.kind === "code");
     :                       ^^^^^^^
 323 |     const vaultItems = result!.index.items.filter((item) => item.metadata.kind === "vault");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:323:24]
 322 |     const codeItems = result!.index.items.filter((item) => item.metadata.kind === "code");
 323 |     const vaultItems = result!.index.items.filter((item) => item.metadata.kind === "vault");
     :                        ^^^^^^^
 324 |     expect(codeItems).toHaveLength(0);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:327:30]
 326 |     // fileHashes should only contain the vault file
 327 |     const keys = Object.keys(result!.index.fileHashes);
     :                              ^^^^^^^
 328 |     expect(keys.every((k) => k.startsWith("vault/"))).toBe(true);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:394:28]
 393 |     expect(files).toHaveLength(1);
 394 |     expect(path.isAbsolute(files[0]!.path)).toBe(true);
     :                            ^^^^^^^^^
 395 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:406:12]
 405 |     expect(files).toHaveLength(1);
 406 |     expect(files[0]!.path.endsWith("readme.md") || files[0]!.path.includes("readme.md")).toBe(true);
     :            ^^^^^^^^^
 407 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:406:52]
 405 |     expect(files).toHaveLength(1);
 406 |     expect(files[0]!.path.endsWith("readme.md") || files[0]!.path.includes("readme.md")).toBe(true);
     :                                                    ^^^^^^^^^
 407 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:446:35]
 445 |     // All fileHashes keys for external files should be absolute
 446 |     for (const key of Object.keys(result!.index.fileHashes)) {
     :                                   ^^^^^^^
 447 |       expect(path.isAbsolute(key)).toBe(true);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:451:27]
 450 |     // All external item sourcePaths should be absolute
 451 |     const externalItems = result!.index.items.filter((item) => item.metadata.kind === "external");
     :                           ^^^^^^^
 452 |     expect(externalItems.length).toBeGreaterThan(0);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:490:27]
 489 |     expect(result).not.toBeNull();
 490 |     const externalItems = result!.index.items.filter((item) => item.metadata.kind === "external");
     :                           ^^^^^^^
 491 |     expect(externalItems.length).toBeGreaterThan(0);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:504:27]
 503 |     expect(result).not.toBeNull();
 504 |     const externalItems = result!.index.items.filter((item) => item.metadata.kind === "external");
     :                           ^^^^^^^
 505 |     expect(externalItems.length).toBeGreaterThan(0);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:518:12]
 517 |     expect(first).not.toBeNull();
 518 |     expect(first!.index.items.some((item) => String(item.metadata.sourcePath).includes("b.ts"))).toBe(true);
     :            ^^^^^^
 519 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:529:12]
 528 |     // b.ts chunks should be gone
 529 |     expect(second!.index.items.some((item) => String(item.metadata.sourcePath).includes("b.ts"))).toBe(false);
     :            ^^^^^^^
 530 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:557:12]
 556 |     expect(result).not.toBeNull();
 557 |     expect(result!.index.items.some((item) => item.metadata.kind === "vault")).toBe(true);
     :            ^^^^^^^
 558 |     expect(result!.index.items.some((item) => item.metadata.kind === "code")).toBe(true);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:558:12]
 557 |     expect(result!.index.items.some((item) => item.metadata.kind === "vault")).toBe(true);
 558 |     expect(result!.index.items.some((item) => item.metadata.kind === "code")).toBe(true);
     :            ^^^^^^^
 559 |     expect(result!.index.items.some((item) => item.metadata.kind === "external")).toBe(true);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/unified-index-pipeline.test.ts:559:12]
 558 |     expect(result!.index.items.some((item) => item.metadata.kind === "code")).toBe(true);
 559 |     expect(result!.index.items.some((item) => item.metadata.kind === "external")).toBe(true);
     :            ^^^^^^^
 560 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x typescript-eslint(consistent-type-imports): `import()` type annotations are forbidden.
    ,-[tests/unified-index-pipeline.test.ts:12:5]
 11 |   const actual = await vi.importActual<
 12 |     typeof import("../src/runtime/vault-embeddings.js")
    :     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 13 |   >("../src/runtime/vault-embeddings.js");
    `----

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
    ,-[tests/unified-index-pipeline.test.ts:27:18]
 26 | function makeVector(seed: number): number[] {
 27 |   const vector = new Array<number>(384).fill(0);
    :                  ^^^^^^^^^^^^^^^^^^^^^^
 28 |   vector[0] = seed;
    `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
     ,-[tests/unified-index-pipeline.test.ts:435:39]
 434 |     mockedEmbedBatch.mockImplementation(async (texts: string[]) =>
 435 |       texts.map((_, i) => { const v = new Array<number>(384).fill(0); v[0] = i + 1; return v; }),
     :                                       ^^^^^^^^^^^^^^^^^^^^^^
 436 |     );
     `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
     ,-[tests/unified-index-pipeline.test.ts:468:19]
 467 |       texts.map(() => {
 468 |         const v = new Array<number>(384).fill(0);
     :                   ^^^^^^^^^^^^^^^^^^^^^^
 469 |         v[0] = vectorSeed++;
     `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  ! eslint(no-magic-numbers): No magic number: 16
     ,-[src/runtime/embedding-ipc-protocol.ts:113:15]
 112 |     .digest("hex")
 113 |     .slice(0, 16);
     :               ^^
 114 | }
     `----

  x eslint(max-params): Function 'handleClaudeHook' has too many parameters (4). Maximum allowed is 3.
    ,-[src/claude/hook-handler.ts:57:39]
 56 |     // oxlint-disable-next-line max-params, max-lines-per-function -- eventName, input, cwd, explicitPlanPath are distinct hook dispatch params; exhaustive event-name dispatch with thin branches
 57 | ,-> export async function handleClaudeHook(
 58 | |     eventName: string,
 59 | |     input: ClaudeHookInput,
 60 | |     cwd: string,
 61 | |     explicitPlanPath?: string
 62 | `-> ): Promise<Record<string, unknown>> {
 63 |       const hookDone = hfLogTimed({ tag: "claude-hook", msg: `handleClaudeHook(${eventName})` });
    `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  ! eslint(no-console): eslint(no-console): Unexpected console statement.
     ,-[src/runtime/unified-store.ts:210:7]
 209 |     if (error instanceof SyntaxError) {
 210 |       console.error(
     :       ^^^^^^^^^^^^^
 211 |         `[unified-store] Corrupted index file, rebuilding from scratch: ${jsonPath}`,
     `----
  help: Delete this console statement.

  ! eslint(no-console): eslint(no-console): Unexpected console statement.
     ,-[src/runtime/unified-store.ts:235:5]
 234 |     // on the next invocation.
 235 |     console.warn(
     :     ^^^^^^^^^^^^
 236 |       `[unified-store] Could not acquire index lock within ${LOCK_TIMEOUT_MS}ms — skipping save (index will be rebuilt next run).`,
     `----
  help: Delete this console statement.

  ! eslint(no-magic-numbers): No magic number: 1.5
    ,-[src/runtime/unified-store.ts:58:36]
 57 |     // Mild back-off, capped so we don't stall too long on the last attempt.
 58 |     interval = Math.min(interval * 1.5, 200);
    :                                    ^^^
 59 |   }
    `----

  ! eslint(no-magic-numbers): No magic number: 200
    ,-[src/runtime/unified-store.ts:58:41]
 57 |     // Mild back-off, capped so we don't stall too long on the last attempt.
 58 |     interval = Math.min(interval * 1.5, 200);
    :                                         ^^^
 59 |   }
    `----

  ! eslint(no-magic-numbers): No magic number: 384
     ,-[src/runtime/unified-store.ts:109:17]
 108 |   version: 2;
 109 |   embeddingDim: 384;
     :                 ^^^
 110 |   items: IndexItem[];
     `----

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/unified-store.ts:326:19]
 325 |   for (let i = 0; i < index.items.length; i++) {
 326 |     idToIndex.set(index.items[i]!.id, i);
     :                   ^^^^^^^^^^^^^^^
 327 |   }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/unified-store.ts:382:18]
 381 |   for (let i = 0; i < index.items.length; i++) {
 382 |     const item = index.items[i]!;
     :                  ^^^^^^^^^^^^^^^
 383 |     if (idSet.has(item.id)) {
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/unified-store.ts:416:18]
 415 |   for (let i = 0; i < index.items.length; i++) {
 416 |     const item = index.items[i]!;
     :                  ^^^^^^^^^^^^^^^
 417 |     if (sourceFilter && !matchesSourceFilter(String(item.metadata.kind ?? ""), sourceFilter)) continue;
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint(max-lines-per-function): The async function main has too many lines (90). Maximum allowed is 50.
     ,-[src/bin/hf-mcp-server.ts:62:1]
  61 |     
  62 | ,-> async function main(): Promise<void> {
  63 | |     let runtime: HybridLoopRuntime | null = null;
  64 | |   
  65 | |     async function getRuntime(): Promise<HybridLoopRuntime> {
  66 | |       if (runtime) return runtime;
  67 | |   
  68 | |       const rt = new HybridLoopRuntime();
  69 | |       const planPath = await resolveLastActivePlanPath(process.cwd());
  70 | |       if (planPath) {
  71 | |         try {
  72 | |           await rt.hydrate(planPath);
  73 | |           runtime = rt;
  74 | |           return runtime;
  75 | |         } catch { /* fall through to planless */ }
  76 | |       }
  77 | |       await rt.hydratePlanless(process.cwd());
  78 | |       runtime = rt;
  79 | |       return runtime;
  80 | |     }
  81 | |   
  82 | |     const rl = readline.createInterface({ input: process.stdin, terminal: false });
  83 | |   
  84 | |     rl.on("line", (line: string) => {
  85 | |       const trimmed = line.trim();
  86 | |       if (!trimmed) return;
  87 | |   
  88 | |       let req: JsonRpcRequest;
  89 | |       try {
  90 | |         req = JSON.parse(trimmed) as JsonRpcRequest;
  91 | |       } catch {
  92 | |         respondError(null, -32700, "Parse error");
  93 | |         return;
  94 | |       }
  95 | |   
  96 | |       const id = req.id ?? null;
  97 | |       const method = req.method;
  98 | |   
  99 | |       if (method === "initialize") {
 100 | |         respond(id, {
 101 | |           protocolVersion: "2024-11-05",
 102 | |           serverInfo: { name: "hf-mcp-server", version: "1.0.0" },
 103 | |           capabilities: { tools: {} }
 104 | |         });
 105 | |         return;
 106 | |       }
 107 | |   
 108 | |       if (method === "notifications/initialized") {
 109 | |         // No response for notifications
 110 | |         return;
 111 | |       }
 112 | |   
 113 | |       if (method === "tools/list") {
 114 | |         respond(id, { tools: [HF_SEARCH_TOOL] });
 115 | |         return;
 116 | |       }
 117 | |   
 118 | |       if (method === "tools/call") {
 119 | |         const params = req.params ?? {};
 120 | |         const name = params["name"] as string | undefined;
 121 | |   
 122 | |         if (name !== "hf_search") {
 123 | |           respondError(id, -32601, `Unknown tool: ${name ?? "(none)"}`);
 124 | |           return;
 125 | |         }
 126 | |   
 127 | |         const args = (params["arguments"] ?? {}) as Record<string, unknown>;
 128 | |         const query = typeof args["query"] === "string" ? args["query"] : "";
 129 | |         if (!query) {
 130 | |           respondError(id, -32602, "Missing required parameter: query");
 131 | |           return;
 132 | |         }
 133 | |   
 134 | |         const topK =
 135 | |           typeof args["top_k"] === "number" && !Number.isNaN(args["top_k"])
 136 | |             ? (args["top_k"] as number)
 137 | |             : 5;
 138 | |   
 139 | |         const sourceArg = args["source"];
 140 | |         const sourceFilter: "vault" | "code" | undefined =
 141 | |           sourceArg === "vault" ? "vault" : sourceArg === "code" ? "code" : undefined;
 142 | |   
 143 | |         getRuntime()
 144 | |           .then(async (rt) => {
 145 | |             const results = await rt.queryIndex(query, topK, sourceFilter);
 146 | |             let text: string;
 147 | |             if (results === null) {
 148 | |               text = "No index available — the unified index has not been built yet for this session.";
 149 | |             } else if (results.length === 0) {
 150 | |               text = "No results found.";
 151 | |             } else {
 152 | |               text = formatToolSearchResults(results);
 153 | |             }
 154 | |             respond(id, { content: [{ type: "text", text }] });
 155 | |           })
 156 | |           .catch((err: unknown) => {
 157 | |             respondError(id, -32603, `Internal error: ${(err as Error).message}`);
 158 | |           });
 159 | |   
 160 | |         return;
 161 | |       }
 162 | |   
 163 | |       respondError(id, -32601, `Method not found: ${method}`);
 164 | |     });
 165 | |   
 166 | |     rl.on("close", () => {
 167 | |       process.exit(0);
 168 | |     });
 169 | `-> }
 170 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines-per-function): The function has too many lines (67). Maximum allowed is 50.
     ,-[src/bin/hf-mcp-server.ts:84:17]
  83 |     
  84 | ,->   rl.on("line", (line: string) => {
  85 | |       const trimmed = line.trim();
  86 | |       if (!trimmed) return;
  87 | |   
  88 | |       let req: JsonRpcRequest;
  89 | |       try {
  90 | |         req = JSON.parse(trimmed) as JsonRpcRequest;
  91 | |       } catch {
  92 | |         respondError(null, -32700, "Parse error");
  93 | |         return;
  94 | |       }
  95 | |   
  96 | |       const id = req.id ?? null;
  97 | |       const method = req.method;
  98 | |   
  99 | |       if (method === "initialize") {
 100 | |         respond(id, {
 101 | |           protocolVersion: "2024-11-05",
 102 | |           serverInfo: { name: "hf-mcp-server", version: "1.0.0" },
 103 | |           capabilities: { tools: {} }
 104 | |         });
 105 | |         return;
 106 | |       }
 107 | |   
 108 | |       if (method === "notifications/initialized") {
 109 | |         // No response for notifications
 110 | |         return;
 111 | |       }
 112 | |   
 113 | |       if (method === "tools/list") {
 114 | |         respond(id, { tools: [HF_SEARCH_TOOL] });
 115 | |         return;
 116 | |       }
 117 | |   
 118 | |       if (method === "tools/call") {
 119 | |         const params = req.params ?? {};
 120 | |         const name = params["name"] as string | undefined;
 121 | |   
 122 | |         if (name !== "hf_search") {
 123 | |           respondError(id, -32601, `Unknown tool: ${name ?? "(none)"}`);
 124 | |           return;
 125 | |         }
 126 | |   
 127 | |         const args = (params["arguments"] ?? {}) as Record<string, unknown>;
 128 | |         const query = typeof args["query"] === "string" ? args["query"] : "";
 129 | |         if (!query) {
 130 | |           respondError(id, -32602, "Missing required parameter: query");
 131 | |           return;
 132 | |         }
 133 | |   
 134 | |         const topK =
 135 | |           typeof args["top_k"] === "number" && !Number.isNaN(args["top_k"])
 136 | |             ? (args["top_k"] as number)
 137 | |             : 5;
 138 | |   
 139 | |         const sourceArg = args["source"];
 140 | |         const sourceFilter: "vault" | "code" | undefined =
 141 | |           sourceArg === "vault" ? "vault" : sourceArg === "code" ? "code" : undefined;
 142 | |   
 143 | |         getRuntime()
 144 | |           .then(async (rt) => {
 145 | |             const results = await rt.queryIndex(query, topK, sourceFilter);
 146 | |             let text: string;
 147 | |             if (results === null) {
 148 | |               text = "No index available — the unified index has not been built yet for this session.";
 149 | |             } else if (results.length === 0) {
 150 | |               text = "No results found.";
 151 | |             } else {
 152 | |               text = formatToolSearchResults(results);
 153 | |             }
 154 | |             respond(id, { content: [{ type: "text", text }] });
 155 | |           })
 156 | |           .catch((err: unknown) => {
 157 | |             respondError(id, -32603, `Internal error: ${(err as Error).message}`);
 158 | |           });
 159 | |   
 160 | |         return;
 161 | |       }
 162 | |   
 163 | |       respondError(id, -32601, `Method not found: ${method}`);
 164 | `->   });
 165 |     
     `----
  help: Consider splitting it into smaller functions.

  ! eslint(no-magic-numbers): No magic number: -32700
    ,-[src/bin/hf-mcp-server.ts:92:26]
 91 |     } catch {
 92 |       respondError(null, -32700, "Parse error");
    :                          ^^^^^^
 93 |       return;
    `----

  ! eslint(no-magic-numbers): No magic number: -32601
     ,-[src/bin/hf-mcp-server.ts:123:26]
 122 |       if (name !== "hf_search") {
 123 |         respondError(id, -32601, `Unknown tool: ${name ?? "(none)"}`);
     :                          ^^^^^^
 124 |         return;
     `----

  ! eslint(no-magic-numbers): No magic number: -32602
     ,-[src/bin/hf-mcp-server.ts:130:26]
 129 |       if (!query) {
 130 |         respondError(id, -32602, "Missing required parameter: query");
     :                          ^^^^^^
 131 |         return;
     `----

  ! eslint(no-magic-numbers): No magic number: 5
     ,-[src/bin/hf-mcp-server.ts:137:13]
 136 |           ? (args["top_k"] as number)
 137 |           : 5;
     :             ^
 138 | 
     `----

  ! eslint(no-magic-numbers): No magic number: -32603
     ,-[src/bin/hf-mcp-server.ts:157:28]
 156 |         .catch((err: unknown) => {
 157 |           respondError(id, -32603, `Internal error: ${(err as Error).message}`);
     :                            ^^^^^^
 158 |         });
     `----

  ! eslint(no-magic-numbers): No magic number: -32601
     ,-[src/bin/hf-mcp-server.ts:163:22]
 162 | 
 163 |     respondError(id, -32601, `Method not found: ${method}`);
     :                      ^^^^^^
 164 |   });
     `----

  x eslint(no-nested-ternary): Do not nest ternary expressions.
     ,-[src/bin/hf-mcp-server.ts:141:9]
 140 |       const sourceFilter: "vault" | "code" | undefined =
 141 |         sourceArg === "vault" ? "vault" : sourceArg === "code" ? "code" : undefined;
     :         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 142 | 
     `----

  x eslint(max-lines-per-function): The function buildResumePrompt has too many lines (166). Maximum allowed is 50.
     ,-[src/runtime/prompt.ts:200:8]
 199 |     
 200 | ,-> export function buildResumePrompt(
 201 | |     plan: ParsedPlan,
 202 | |     status: RuntimeStatus,
 203 | |     vault: VaultContext | null = null,
 204 | |     vaultSearchResults: VaultSearchResult[] | null = null,
 205 | |     budgets: PromptBudgets = {}
 206 | |   ): string {
 207 | |     const charBudget = budgets.charBudget ?? DEFAULT_VAULT_CHAR_BUDGET;
 208 | |     const planningBudget = budgets.planningCharBudget ?? PLANNING_VAULT_CHAR_BUDGET;
 209 | |     const currentMilestone = plan.currentMilestone;
 210 | |   
 211 | |     if (!currentMilestone && plan.completed) {
 212 | |       return `Plan ${plan.slug} is complete. Verify the completed milestones and stop.`;
 213 | |     }
 214 | |   
 215 | |     if (status.phase === "planning") {
 216 | |       const lines = [
 217 | |         `Continue the managed Hybrid Framework planning-review loop for plan ${plan.path}.`,
 218 | |         "",
 219 | |         "## Plan status",
 220 | |         `Plan status: ${plan.status}.`,
 221 | |         `Loop attempts: ${status.counters.totalAttempts}/${status.counters.maxTotalTurns}.`,
 222 | |         `Evaluated outcomes: ${status.counters.totalTurns}.`,
 223 | |         "",
 224 | |         "## User intent"
 225 | |       ];
 226 | |   
 227 | |       if (plan.userIntent) {
 228 | |         lines.push(plan.userIntent);
 229 | |       } else {
 230 | |         lines.push("User intent is missing from the plan doc. Add a dedicated `## User Intent` section before requesting approval.");
 231 | |       }
 232 | |   
 233 | |       lines.push("");
 234 | |       lines.push("## Planning gate");
 235 | |       lines.push("The runtime owns the loop between `hf-planner` and `hf-plan-reviewer` until the plan is approved.");
 236 | |       lines.push("Expand the request into explicit milestones, then send the full context and draft plan to `hf-plan-reviewer`.");
 237 | |       lines.push("Do not hand off to `hf-builder` until the reviewer approves the plan.");
 238 | |       lines.push("");
 239 | |   
 240 | |       if (status.lastOutcome) {
 241 | |         lines.push("## Last turn");
 242 | |         lines.push(`State: ${status.lastOutcome.state}`);
 243 | |         lines.push(`Summary: ${status.lastOutcome.summary}`);
 244 | |         lines.push("");
 245 | |       }
 246 | |   
 247 | |       lines.push(...formatRuntimeState(status));
 248 | |   
 249 | |       if (status.lastBlocker) {
 250 | |         lines.push("## Active blocker");
 251 | |         lines.push(`${status.lastBlocker.message} (repeated ${status.counters.repeatedBlocker} times).`);
 252 | |         lines.push("");
 253 | |       }
 254 | |   
 255 | |       const recoveryLines = formatRecoveryContext(status);
 256 | |       if (recoveryLines.length > 0) {
 257 | |         lines.push(...recoveryLines);
 258 | |       }
 259 | |   
 260 | |       if (vault && (vault.plan.length > 0 || vault.shared.length > 0)) {
 261 | |         lines.push("## Exploration state");
 262 | |         lines.push("The following vault documents contain findings from prior exploration passes:");
 263 | |         for (const doc of vault.plan) {
 264 | |           lines.push(`- **${doc.title}**: \`${doc.path}\``);
 265 | |         }
 266 | |         for (const doc of vault.shared) {
 267 | |           lines.push(`- **${doc.title}**: \`${doc.path}\``);
 268 | |         }
 269 | |         lines.push("");
 270 | |       }
 271 | |   
 272 | |       const planningVaultLines = vaultSearchResults && vaultSearchResults.length > 0
 273 | |         ? formatSemanticVaultContext(vaultSearchResults, planningBudget)
 274 | |         : formatVaultContext(vault, planningBudget);
 275 | |       if (planningVaultLines.length > 0) {
 276 | |         lines.push(...planningVaultLines);
 277 | |       }
 278 | |   
 279 | |       lines.push("## Instructions");
 280 | |       lines.push(
 281 | |         status.recommendedNextAction
 282 | |           ? `Recommended next action: ${status.recommendedNextAction}`
 283 | |           : "Recommended next action: revise the draft plan until `hf-plan-reviewer` can approve it."
 284 | |       );
 285 | |       lines.push("Reference vault-persisted findings and the plan doc; avoid re-loading the full context bundle into conversation.");
 286 | |       lines.push("Emit a final turn_outcome trailer as the last block of your response:");
 287 | |       lines.push("");
 288 | |       lines.push(TURN_OUTCOME_TRAILER_FORMAT);
 289 | |   
 290 | |       return lines.join("\n");
 291 | |     }
 292 | |   
 293 | |     if (!currentMilestone) {
 294 | |       return [
 295 | |         `Continue the managed Hybrid Framework loop for plan ${plan.path}.`,
 296 | |         "",
 297 | |         "## Progress",
 298 | |         `Milestones: ${plan.milestones.length}/${plan.milestones.length} complete.`,
 299 | |         `Plan status: ${plan.status}.`,
 300 | |         "",
 301 | |         "## Final verification",
 302 | |         "All milestones are checked, but final verification evidence still needs to be attached before the plan can move to `status: complete`.",
 303 | |         "",
 304 | |         "## Instructions",
 305 | |         "Run the narrowest final verification needed for the completed work, attach fresh evidence under the last completed milestone, then update the plan status only if verification passes.",
 306 | |         "Emit a final turn_outcome trailer as the last block of your response:",
 307 | |         "",
 308 | |         TURN_OUTCOME_TRAILER_FORMAT
 309 | |       ].join("\n");
 310 | |     }
 311 | |   
 312 | |     const completedCount = plan.milestones.filter((m) => m.checked).length;
 313 | |     const totalCount = plan.milestones.length;
 314 | |   
 315 | |     const lines = [
 316 | |       `Continue the managed Hybrid Framework loop for plan ${plan.path}.`,
 317 | |       "",
 318 | |       "## Progress",
 319 | |       `Milestones: ${completedCount}/${totalCount} complete.`,
 320 | |       `Loop attempts: ${status.counters.totalAttempts}/${status.counters.maxTotalTurns}.`,
 321 | |       `Evaluated outcomes: ${status.counters.totalTurns}.`,
 322 | |       "",
 323 | |       "## Current milestone",
 324 | |       `${currentMilestone.index}. ${currentMilestone.title}`,
 325 | |       `Full text: ${currentMilestone.text}`,
 326 | |       ...formatMilestoneContext(currentMilestone),
 327 | |       ""
 328 | |     ];
 329 | |   
 330 | |     const vaultLines = vaultSearchResults && vaultSearchResults.length > 0
 331 | |       ? formatSemanticVaultContext(vaultSearchResults, charBudget)
 332 | |       : formatVaultContext(vault, charBudget);
 333 | |     if (vaultLines.length > 0) {
 334 | |       lines.push(...vaultLines);
 335 | |     }
 336 | |   
 337 | |     if (status.lastOutcome) {
 338 | |       lines.push("## Last turn");
 339 | |       lines.push(`State: ${status.lastOutcome.state}`);
 340 | |       lines.push(`Summary: ${status.lastOutcome.summary}`);
 341 | |       if (status.lastOutcome.files_changed.length > 0) {
 342 | |         lines.push(`Files changed: ${status.lastOutcome.files_changed.join(", ")}`);
 343 | |       }
 344 | |     } else {
 345 | |       lines.push("## Last turn");
 346 | |       lines.push("No outcome recorded yet.");
 347 | |     }
 348 | |     lines.push("");
 349 | |   
 350 | |     if (status.lastVerification) {
 351 | |       lines.push(`## Verification: ${status.lastVerification.status}`);
 352 | |       if (status.lastVerification.summary) {
 353 | |         lines.push(status.lastVerification.summary);
 354 | |       }
 355 | |       lines.push("");
 356 | |     }
 357 | |   
 358 | |     lines.push(...formatRuntimeState(status));
 359 | |   
 360 | |     if (status.lastBlocker) {
 361 | |       lines.push("## Active blocker");
 362 | |       lines.push(`${status.lastBlocker.message} (repeated ${status.counters.repeatedBlocker} times).`);
 363 | |       lines.push("");
 364 | |     }
 365 | |   
 366 | |     const recoveryLines = formatRecoveryContext(status);
 367 | |     if (recoveryLines.length > 0) {
 368 | |       lines.push(...recoveryLines);
 369 | |     } else if (status.counters.turnsSinceLastOutcome > 0) {
 370 | |       lines.push(`## Warning: ${status.counters.turnsSinceLastOutcome} stop(s) without a TurnOutcome trailer.`);
 371 | |       lines.push(`Raw loop attempts still count toward the hard limit (${status.counters.totalAttempts}/${status.counters.maxTotalTurns}).`);
 372 | |       lines.push("Emit the canonical turn_outcome trailer before stopping.");
 373 | |       lines.push("");
 374 | |     }
 375 | |   
 376 | |     lines.push("## Instructions");
 377 | |     lines.push(
 378 | |       status.recommendedNextAction
 379 | |         ? `Recommended next action: ${status.recommendedNextAction}`
 380 | |         : "Recommended next action: make the smallest forward progress on the current milestone only."
 381 | |     );
 382 | |     lines.push("Keep scope limited to the current unchecked milestone.");
 383 | |     lines.push("Emit a final turn_outcome trailer as the last block of your response:");
 384 | |     lines.push("");
 385 | |     lines.push(TURN_OUTCOME_TRAILER_FORMAT);
 386 | |   
 387 | |     return lines.join("\n");
 388 | `-> }
 389 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-params): Function 'appendWithBudget' has too many parameters (4). Maximum allowed is 3.
    ,-[src/runtime/prompt.ts:22:26]
 21 |      */
 22 | ,-> function appendWithBudget(
 23 | |     lines: string[],
 24 | |     blocks: string[][],
 25 | |     charBudget: number,
 26 | |     used: number,
 27 | `-> ): void {
 28 |       for (const block of blocks) {
    `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(max-params): Function 'buildResumePrompt' has too many parameters (5). Maximum allowed is 3.
     ,-[src/runtime/prompt.ts:200:34]
 199 |     
 200 | ,-> export function buildResumePrompt(
 201 | |     plan: ParsedPlan,
 202 | |     status: RuntimeStatus,
 203 | |     vault: VaultContext | null = null,
 204 | |     vaultSearchResults: VaultSearchResult[] | null = null,
 205 | |     budgets: PromptBudgets = {}
 206 | `-> ): string {
 207 |       const charBudget = budgets.charBudget ?? DEFAULT_VAULT_CHAR_BUDGET;
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(no-nested-ternary): Do not nest ternary expressions.
     ,-[src/runtime/prompt.ts:159:23]
 158 |       const blocks = results.map((result, index) => {
 159 | ,->     const kindLabel = result.metadata.kind === "code"
 160 | |         ? "[code]"
 161 | |         : result.metadata.kind === "external"
 162 | |         ? "[external]"
 163 | `->       : "[vault]";
 164 |         const num = index + 1;
     `----

  x eslint(no-unused-vars): Type 'VaultDocument' is imported but never used.
    ,-[src/runtime/prompt.ts:10:8]
  9 |   type VaultContext,
 10 |   type VaultDocument,
    :        ^^^^^^|^^^^^^
    :              `-- 'VaultDocument' is imported here
 11 |   type VaultSearchResult,
    `----
  help: Consider removing this import.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/prompt.ts:135:22]
 134 |   const lines = [hasCodeResults ? "## Knowledge context" : "## Vault context"];
 135 |   const headerCost = lines[0]!.length + 1;
     :                      ^^^^^^^^^
 136 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/code-chunker.ts:163:75]
 162 |       for (let i = 0; i < parts.length; i++) {
 163 |         const base = createVaultChunk(filePath, { name: chunk.name, text: parts[i]! });
     :                                                                           ^^^^^^^^^
 164 |         base.id = `${base.id}:${i}`;
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/prompt.test.ts:129:47]
 128 |     const [vaultResult] = makeSearchResults(1);
 129 |     const lines = formatSemanticVaultContext([vaultResult!, makeCodeSearchResult()]);
     :                                               ^^^^^^^^^^^^
 130 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint(max-depth): Blocks are nested too deeply (4). Maximum allowed is 3.
     ,-[src/bin/hf-runtime.ts:333:13]
 332 |               if (codeRoots.length > 0) {
 333 | ,->             for (const root of codeRoots) {
 334 | |                 console.log(`  code:     ${root}`);
 335 | `->             }
 336 |               } else {
     `----
  help: Consider refactoring your code.

  x eslint(max-depth): Blocks are nested too deeply (4). Maximum allowed is 3.
     ,-[src/bin/hf-runtime.ts:341:13]
 340 |               if (externalRoots.length > 0) {
 341 | ,->             for (const root of externalRoots) {
 342 | |                 console.log(`  external: ${root}`);
 343 | `->             }
 344 |               } else {
     `----
  help: Consider refactoring your code.

  x eslint(max-lines-per-function): The async function main has too many lines (288). Maximum allowed is 50.
     ,-[src/bin/hf-runtime.ts:63:1]
  62 |     
  63 | ,-> async function main(): Promise<void> {
  64 | |     const [, , command, ...argv] = process.argv;
  65 | |   
  66 | |     if (!command || command === "help" || command === "--help" || command === "-h") {
  67 | |       printUsage();
  68 | |       process.exitCode = command ? 0 : 1;
  69 | |       return;
  70 | |     }
  71 | |   
  72 | |     const runtime = new HybridLoopRuntime();
  73 | |   
  74 | |     switch (command) {
  75 | |       case "status": {
  76 | |         const planPath = await resolveManagedPlanPath(process.cwd(), parsePlanArg(argv));
  77 | |         const status = await runtime.hydrate(planPath);
  78 | |         console.log(formatStatus(planPath, status));
  79 | |         return;
  80 | |       }
  81 | |   
  82 | |       case "tail": {
  83 | |         const lines = Number.parseInt(parseFlag(argv, "--lines") ?? "20", 10);
  84 | |         const planPath = await resolveManagedPlanPath(process.cwd(), parsePlanArg(argv));
  85 | |         const plan = await parsePlan(planPath);
  86 | |         const runtimePaths = getRuntimePaths(plan);
  87 | |         const eventLines = await readEventLines(runtimePaths);
  88 | |         console.log(eventLines.slice(-lines).join(os.EOL));
  89 | |         return;
  90 | |       }
  91 | |   
  92 | |       case "resume": {
  93 | |         const via = parseFlag(argv, "--via") as RuntimeVendor | undefined;
  94 | |         if (!via || (via !== "opencode" && via !== "claude")) {
  95 | |           throw new Error("--via must be one of: opencode, claude");
  96 | |         }
  97 | |   
  98 | |         const planPath = await resolveManagedPlanPath(process.cwd(), parsePlanArg(argv));
  99 | |         await runtime.hydrate(planPath);
 100 | |         await runtime.recordEvent({
 101 | |           vendor: via,
 102 | |           type: "session.resume_requested",
 103 | |           timestamp: new Date().toISOString()
 104 | |         });
 105 | |         const decision = await runtime.decideNext();
 106 | |         console.log(JSON.stringify(decision, null, 2));
 107 | |         return;
 108 | |       }
 109 | |   
 110 | |       case "doctor": {
 111 | |         const planPath = await resolveManagedPlanPath(process.cwd(), parsePlanArg(argv));
 112 | |         const result = await runDoctor(planPath);
 113 | |         console.log(JSON.stringify(result, null, 2));
 114 | |         process.exitCode = result.ok ? 0 : 1;
 115 | |         return;
 116 | |       }
 117 | |   
 118 | |       case "outcome": {
 119 | |         const json = parseFlag(argv, "--json");
 120 | |         if (!json) {
 121 | |           throw new Error("Missing --json for outcome command.");
 122 | |         }
 123 | |   
 124 | |         const planPath = await resolveManagedPlanPath(process.cwd(), parsePlanArg(argv));
 125 | |         await runtime.hydrate(planPath);
 126 | |         const parsed = parseTurnOutcomeInput(json);
 127 | |         if (parsed.kind !== "valid") {
 128 | |           await runtime.recordOutcomeIngestionIssue(buildTurnOutcomeIngestionEvent({
 129 | |             vendor: "runtime",
 130 | |             source: "hf-runtime outcome --json",
 131 | |             result: parsed
 132 | |           }));
 133 | |           throw new Error(parsed.errors.map((error) => `${error.path}: ${error.message}`).join("; "));
 134 | |         }
 135 | |   
 136 | |         const status = await runtime.evaluateTurn(parsed.outcome);
 137 | |         const decision = await runtime.decideNext();
 138 | |         console.log(JSON.stringify({ status, decision }, null, 2));
 139 | |         return;
 140 | |       }
 141 | |   
 142 | |       case "search": {
 143 | |         const query = parsePlanArg(argv);
 144 | |         if (!query) {
 145 | |           throw new Error("Missing query text. Usage: hf-runtime search \"<query>\" [--top-k N] [--source vault|code|all]");
 146 | |         }
 147 | |   
 148 | |         const topKRaw = parseFlag(argv, "--top-k");
 149 | |         const topK = topKRaw !== undefined ? Number.parseInt(topKRaw, 10) : 5;
 150 | |   
 151 | |         const sourceRaw = parseFlag(argv, "--source");
 152 | |         const sourceFilter: "vault" | "code" | undefined =
 153 | |           sourceRaw === "vault" ? "vault"
 154 | |           : sourceRaw === "code" ? "code"
 155 | |           : undefined;
 156 | |   
 157 | |         await runtime.hydratePlanless(process.cwd());
 158 | |         const results = await runtime.queryIndex(query, topK, sourceFilter);
 159 | |   
 160 | |         if (results === null) {
 161 | |           console.log("No index available. Run a plan or build the index first.");
 162 | |           return;
 163 | |         }
 164 | |   
 165 | |         if (results.length === 0) {
 166 | |           console.log("No results found.");
 167 | |           return;
 168 | |         }
 169 | |   
 170 | |         console.log(formatToolSearchResults(results));
 171 | |         return;
 172 | |       }
 173 | |   
 174 | |       case "index": {
 175 | |         const subCmd = argv[0];
 176 | |         argv.shift();
 177 | |   
 178 | |         const repoRoot = process.cwd();
 179 | |         const configPath = path.join(repoRoot, "hybrid-framework.json");
 180 | |   
 181 | |         function readHfConfig(): Record<string, unknown> {
 182 | |           try {
 183 | |             return JSON.parse(fs.readFileSync(configPath, "utf8")) as Record<string, unknown>;
 184 | |           } catch (error) {
 185 | |             if ((error as NodeJS.ErrnoException).code === "ENOENT") {
 186 | |               return {};
 187 | |             }
 188 | |             throw error;
 189 | |           }
 190 | |         }
 191 | |   
 192 | |         function writeHfConfig(config: Record<string, unknown>): void {
 193 | |           fs.writeFileSync(configPath, `${JSON.stringify(config, null, 2)}\n`, "utf8");
 194 | |         }
 195 | |   
 196 | |         function getExternalRoots(config: Record<string, unknown>): string[] {
 197 | |           const idx = config.index;
 198 | |           if (idx === null || typeof idx !== "object" || Array.isArray(idx)) return [];
 199 | |           const idxRecord = idx as Record<string, unknown>;
 200 | |           const ext = idxRecord.external;
 201 | |           if (ext === null || typeof ext !== "object" || Array.isArray(ext)) return [];
 202 | |           const extRecord = ext as Record<string, unknown>;
 203 | |           const roots = extRecord.roots;
 204 | |           if (!Array.isArray(roots)) return [];
 205 | |           return roots.filter((r): r is string => typeof r === "string");
 206 | |         }
 207 | |   
 208 | |         function ensureExternalConfig(config: Record<string, unknown>): void {
 209 | |           if (config.index === null || typeof config.index !== "object" || Array.isArray(config.index)) {
 210 | |             config.index = { external: { roots: [] } };
 211 | |             return;
 212 | |           }
 213 | |           const idx = config.index as Record<string, unknown>;
 214 | |           if (idx.external === null || typeof idx.external !== "object" || Array.isArray(idx.external)) {
 215 | |             idx.external = { roots: [] };
 216 | |             return;
 217 | |           }
 218 | |           const ext = idx.external as Record<string, unknown>;
 219 | |           if (!Array.isArray(ext.roots)) {
 220 | |             ext.roots = [];
 221 | |           }
 222 | |         }
 223 | |   
 224 | |         switch (subCmd) {
 225 | |           case "add": {
 226 | |             const rawPath = parsePlanArg(argv);
 227 | |             if (!rawPath) {
 228 | |               throw new Error("Missing path argument. Usage: hf-runtime index add <path> [--extensions .ts,.js]");
 229 | |             }
 230 | |   
 231 | |             const absPath = path.resolve(rawPath);
 232 | |             if (!fs.existsSync(absPath)) {
 233 | |               console.error(`Error: path does not exist: ${absPath}`);
 234 | |               process.exitCode = 1;
 235 | |               return;
 236 | |             }
 237 | |   
 238 | |             const extensionsRaw = parseFlag(argv, "--extensions");
 239 | |             const extensions = extensionsRaw
 240 | |               ? extensionsRaw.split(",").map((e) => e.trim()).filter(Boolean)
 241 | |               : undefined;
 242 | |   
 243 | |             const config = readHfConfig();
 244 | |             ensureExternalConfig(config);
 245 | |   
 246 | |             const idx = config.index as Record<string, unknown>;
 247 | |             const ext = idx.external as Record<string, unknown>;
 248 | |             const roots = ext.roots as string[];
 249 | |   
 250 | |             if (roots.includes(absPath)) {
 251 | |               console.log(`Already indexed: ${absPath}`);
 252 | |               return;
 253 | |             }
 254 | |   
 255 | |             roots.push(absPath);
 256 | |   
 257 | |             if (extensions) {
 258 | |               ext.extensions = extensions;
 259 | |             }
 260 | |   
 261 | |             writeHfConfig(config);
 262 | |             console.log(`Added to index: ${absPath}`);
 263 | |   
 264 | |             // Trigger rebuild
 265 | |             console.log("Rebuilding index...");
 266 | |             const indexConfig = await loadIndexConfig(repoRoot);
 267 | |             const externalRoots = getExternalRoots(readHfConfig());
 268 | |             const addExtensions = extensions ?? DEFAULT_INDEX_CONFIG.external?.extensions;
 269 | |             const addExclude = DEFAULT_INDEX_CONFIG.external?.exclude;
 270 | |             const result = await buildUnifiedIndex({
 271 | |               repoRoot,
 272 | |               codeConfig: indexConfig.code.enabled !== false
 273 | |                 ? {
 274 | |                   roots: indexConfig.code.roots,
 275 | |                   extensions: indexConfig.code.extensions,
 276 | |                   exclude: indexConfig.code.exclude,
 277 | |                 }
 278 | |                 : undefined,
 279 | |               externalConfig: externalRoots.length > 0
 280 | |                 ? {
 281 | |                   roots: externalRoots,
 282 | |                   ...(addExtensions ? { extensions: addExtensions } : {}),
 283 | |                   ...(addExclude ? { exclude: addExclude } : {}),
 284 | |                 }
 285 | |                 : undefined,
 286 | |               embeddingBatchSize: indexConfig.embeddingBatchSize,
 287 | |               maxChunkChars: indexConfig.maxChunkChars,
 288 | |             });
 289 | |             if (result) {
 290 | |               console.log(`Index rebuilt: ${result.index.items.length} chunks indexed.`);
 291 | |             } else {
 292 | |               console.log("Index rebuild complete (no files found).");
 293 | |             }
 294 | |             return;
 295 | |           }
 296 | |   
 297 | |           case "remove": {
 298 | |             const rawPath = parsePlanArg(argv);
 299 | |             if (!rawPath) {
 300 | |               throw new Error("Missing path argument. Usage: hf-runtime index remove <path>");
 301 | |             }
 302 | |   
 303 | |             const absPath = path.resolve(rawPath);
 304 | |             const config = readHfConfig();
 305 | |             const roots = getExternalRoots(config);
 306 | |   
 307 | |             if (!roots.includes(absPath)) {
 308 | |               console.error(`Error: path not found in index roots: ${absPath}`);
 309 | |               process.exitCode = 1;
 310 | |               return;
 311 | |             }
 312 | |   
 313 | |             ensureExternalConfig(config);
 314 | |             const idx = config.index as Record<string, unknown>;
 315 | |             const ext = idx.external as Record<string, unknown>;
 316 | |             ext.roots = (ext.roots as string[]).filter((r) => r !== absPath);
 317 | |   
 318 | |             writeHfConfig(config);
 319 | |             console.log(`Removed from index: ${absPath}`);
 320 | |             return;
 321 | |           }
 322 | |   
 323 | |           case "list": {
 324 | |             const indexConfig = await loadIndexConfig(repoRoot);
 325 | |             const config = readHfConfig();
 326 | |             const externalRoots = getExternalRoots(config);
 327 | |   
 328 | |             console.log("Index roots:");
 329 | |             console.log(`  vault:    ${path.join(repoRoot, "vault")}`);
 330 | |   
 331 | |             const codeRoots = indexConfig.code.roots.map((r) => path.resolve(repoRoot, r));
 332 | |             if (codeRoots.length > 0) {
 333 | |               for (const root of codeRoots) {
 334 | |                 console.log(`  code:     ${root}`);
 335 | |               }
 336 | |             } else {
 337 | |               console.log("  code:     (none configured)");
 338 | |             }
 339 | |   
 340 | |             if (externalRoots.length > 0) {
 341 | |               for (const root of externalRoots) {
 342 | |                 console.log(`  external: ${root}`);
 343 | |               }
 344 | |             } else {
 345 | |               console.log("  external: (none configured)");
 346 | |             }
 347 | |             return;
 348 | |           }
 349 | |   
 350 | |           case "rebuild": {
 351 | |             console.log("Rebuilding index...");
 352 | |             const indexConfig = await loadIndexConfig(repoRoot);
 353 | |             const config = readHfConfig();
 354 | |             const externalRoots = getExternalRoots(config);
 355 | |             const rebuildExtensions = indexConfig.external?.extensions ?? DEFAULT_INDEX_CONFIG.external?.extensions;
 356 | |             const rebuildExclude = indexConfig.external?.exclude ?? DEFAULT_INDEX_CONFIG.external?.exclude;
 357 | |   
 358 | |             const result = await buildUnifiedIndex({
 359 | |               repoRoot,
 360 | |               codeConfig: indexConfig.code.enabled !== false
 361 | |                 ? {
 362 | |                   roots: indexConfig.code.roots,
 363 | |                   extensions: indexConfig.code.extensions,
 364 | |                   exclude: indexConfig.code.exclude,
 365 | |                 }
 366 | |                 : undefined,
 367 | |               externalConfig: externalRoots.length > 0
 368 | |                 ? {
 369 | |                   roots: externalRoots,
 370 | |                   ...(rebuildExtensions ? { extensions: rebuildExtensions } : {}),
 371 | |                   ...(rebuildExclude ? { exclude: rebuildExclude } : {}),
 372 | |                 }
 373 | |                 : undefined,
 374 | |               embeddingBatchSize: indexConfig.embeddingBatchSize,
 375 | |               maxChunkChars: indexConfig.maxChunkChars,
 376 | |             });
 377 | |             if (result) {
 378 | |               console.log(`Index rebuilt: ${result.index.items.length} chunks indexed.`);
 379 | |             } else {
 380 | |               console.log("Index rebuild complete (no files found).");
 381 | |             }
 382 | |             return;
 383 | |           }
 384 | |   
 385 | |           default:
 386 | |             console.error(`Unknown index subcommand: ${subCmd ?? "(none)"}`);
 387 | |             console.error("Available: add, remove, list, rebuild");
 388 | |             process.exitCode = 1;
 389 | |             return;
 390 | |         }
 391 | |       }
 392 | |   
 393 | |       default:
 394 | |         printUsage();
 395 | |         process.exitCode = 1;
 396 | |     }
 397 | `-> }
 398 |     
     `----
  help: Consider splitting it into smaller functions.

  ! eslint(no-magic-numbers): No magic number: 5
     ,-[src/bin/hf-runtime.ts:149:75]
 148 |       const topKRaw = parseFlag(argv, "--top-k");
 149 |       const topK = topKRaw !== undefined ? Number.parseInt(topKRaw, 10) : 5;
     :                                                                           ^
 150 | 
     `----

  x eslint(no-nested-ternary): Do not nest ternary expressions.
     ,-[src/bin/hf-runtime.ts:153:9]
 152 |           const sourceFilter: "vault" | "code" | undefined =
 153 | ,->         sourceRaw === "vault" ? "vault"
 154 | |           : sourceRaw === "code" ? "code"
 155 | `->         : undefined;
 156 |     
     `----

  x eslint(max-lines-per-function): The async function buildUnifiedIndex has too many lines (88). Maximum allowed is 50.
     ,-[src/runtime/unified-index-pipeline.ts:309:8]
 308 |     
 309 | ,-> export async function buildUnifiedIndex(
 310 | |     config: BuildUnifiedIndexConfig,
 311 | |   ): Promise<{ index: UnifiedIndex; vectors: Float32Array } | null> {
 312 | |     const scannedFiles = [...new Map([
 313 | |       ...(config.vaultPaths ? await scanVaultFiles(config.repoRoot, config.vaultPaths, config.vaultContext) : []),
 314 | |       ...(config.codeConfig ? await scanCodeFiles(config.repoRoot, config.codeConfig) : []),
 315 | |       ...(config.externalConfig ? await scanExternalFiles(config.externalConfig) : []),
 316 | |     ].map((file) => [file.path, file] as const)).values()].sort((a, b) => a.path.localeCompare(b.path));
 317 | |   
 318 | |     if (scannedFiles.length === 0) {
 319 | |       const emptyIndex = createEmptyIndex();
 320 | |       return { index: emptyIndex, vectors: new Float32Array() };
 321 | |     }
 322 | |   
 323 | |     const currentHashes = Object.fromEntries(scannedFiles.map((file) => [file.path, file.hash]));
 324 | |     const existing = await loadUnifiedIndex(config.repoRoot);
 325 | |     const previousHashes = existing?.index.fileHashes ?? {};
 326 | |     const currentPaths = new Set(scannedFiles.map((file) => file.path));
 327 | |     const changedOrAdded = scannedFiles.filter((file) => previousHashes[file.path] !== file.hash);
 328 | |     const removedPaths = Object.keys(previousHashes)
 329 | |       .filter((filePath) => !currentPaths.has(filePath))
 330 | |       .sort((a, b) => a.localeCompare(b));
 331 | |   
 332 | |     if (existing && changedOrAdded.length === 0 && removedPaths.length === 0) {
 333 | |       return existing;
 334 | |     }
 335 | |   
 336 | |     let state = existing ?? { index: createEmptyIndex(), vectors: new Float32Array() };
 337 | |     const stalePaths = new Set([...changedOrAdded.map((file) => file.path), ...removedPaths]);
 338 | |     if (stalePaths.size > 0) {
 339 | |       const staleIds = state.index.items
 340 | |         .filter((item) => stalePaths.has(String(item.metadata.sourcePath)))
 341 | |         .map((item) => item.id);
 342 | |       state = deleteItems(state.index, state.vectors, staleIds);
 343 | |     }
 344 | |   
 345 | |     state.index.fileHashes = currentHashes;
 346 | |     state.index.timestamp = new Date().toISOString();
 347 | |   
 348 | |     const changedChunks = changedOrAdded.flatMap((file) => {
 349 | |       if (file.kind === "vault") {
 350 | |         return chunkVaultDocument({
 351 | |           path: file.path,
 352 | |           title: file.title ?? path.basename(file.path, path.extname(file.path)),
 353 | |           content: file.content,
 354 | |         }, config.maxChunkChars);
 355 | |       }
 356 | |       if (file.kind === "external") {
 357 | |         const ext = path.extname(file.path).toLowerCase();
 358 | |         if (ext === ".ts" || ext === ".js") {
 359 | |           // Code chunker produces kind:"code"; override to "external"
 360 | |           return chunkTypeScriptFile(file.path, file.content, config.maxChunkChars).map((chunk) => ({
 361 | |             ...chunk,
 362 | |             metadata: { ...chunk.metadata, kind: "external" as const },
 363 | |           }));
 364 | |         }
 365 | |         if (ext === ".md") {
 366 | |           // Vault chunker produces no kind; override to "external"
 367 | |           return chunkVaultDocument({
 368 | |             path: file.path,
 369 | |             title: path.basename(file.path, ext),
 370 | |             content: file.content,
 371 | |           }, config.maxChunkChars).map((chunk) => ({
 372 | |             ...chunk,
 373 | |             metadata: { ...chunk.metadata, kind: "external" as const },
 374 | |           }));
 375 | |         }
 376 | |         return chunkTextFile(file.path, file.content, config.maxChunkChars);
 377 | |       }
 378 | |       return chunkTypeScriptFile(file.path, file.content, config.maxChunkChars);
 379 | |     });
 380 | |   
 381 | |     if (changedChunks.length === 0) {
 382 | |       await saveUnifiedIndex(config.repoRoot, state.index, state.vectors);
 383 | |       return state;
 384 | |     }
 385 | |   
 386 | |     try {
 387 | |       const embeddings = await embedInBatches(changedChunks.map((chunk) => chunk.text), config.embeddingBatchSize);
 388 | |   
 389 | |       const batchEntries = changedChunks.map((chunk, i) => ({
 390 | |         item: {
 391 | |           id: chunk.id,
 392 | |           text: chunk.text,
 393 | |           metadata: toIndexMetadata(chunk.metadata),
 394 | |         },
 395 | |         vector: embeddings[i]!,
 396 | |       }));
 397 | |   
 398 | |       state = batchUpsertItems(state.index, state.vectors, batchEntries);
 399 | |   
 400 | |       await saveUnifiedIndex(config.repoRoot, state.index, state.vectors);
 401 | |       return state;
 402 | |     } catch (error) {
 403 | |       if (error instanceof EmbeddingModelError) {
 404 | |         hfLog({ tag: "unified-index", msg: "embedding failed, skipping index build", data: { error: error.message } });
 405 | |         return null;
 406 | |       }
 407 | |       throw error;
 408 | |     }
 409 | `-> }
     `----
  help: Consider splitting it into smaller functions.

  ! eslint(no-magic-numbers): No magic number: 16
     ,-[src/runtime/unified-index-pipeline.ts:242:19]
 241 |         .digest("hex")
 242 |         .slice(0, 16);
     :                   ^^
 243 |       chunks.push({
     `----

  ! eslint(no-magic-numbers): No magic number: 16
     ,-[src/runtime/unified-index-pipeline.ts:259:84]
 258 |   if (chunks.length === 0 && content.trim()) {
 259 |     const id = createHash("sha256").update(`${filePath}:0`).digest("hex").slice(0, 16);
     :                                                                                    ^^
 260 |     chunks.push({
     `----

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/unified-index-pipeline.ts:395:15]
 394 |       },
 395 |       vector: embeddings[i]!,
     :               ^^^^^^^^^^^^^^
 396 |     }));
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint(max-lines-per-function): The function createHybridRuntimeHooks has too many lines (379). Maximum allowed is 50.
     ,-[src/opencode/plugin.ts:217:8]
 216 |     
 217 | ,-> export function createHybridRuntimeHooks(context: OpenCodePluginContext): HybridRuntimeHooksResult {
 218 | |     // Per-session runtime map: sessionId → Promise<HybridLoopRuntime | null>
 219 | |     const sessionRuntimes = new Map<string, Promise<HybridLoopRuntime | null>>();
 220 | |   
 221 | |     // Per-session plan bindings: sessionId → explicit plan path.
 222 | |     // Written by hf_plan_start tool (M2); read by getRuntime().
 223 | |     const planBindings = new Map<string, string>();
 224 | |   
 225 | |     // Per-session ESC-interrupt and agent-gate flags.
 226 | |     // Set by "message.updated" and consumed by session.idle / gated hooks.
 227 | |     //
 228 | |     // Expected event shape (from @opencode-ai/sdk EventMessageUpdated):
 229 | |     //   event.type === "message.updated"
 230 | |     //   event.properties.info: Message (UserMessage | AssistantMessage)
 231 | |     //
 232 | |     // AssistantMessage shape (relevant fields):
 233 | |     //   { role: "assistant", agent: string, error?: { name: "MessageAbortedError", ... } | ... }
 234 | |     //
 235 | |     // UserMessage shape (relevant fields):
 236 | |     //   { role: "user", agent: string }
 237 | |     //
 238 | |     // Interrupt signal: role === "assistant" && error?.name === "MessageAbortedError"
 239 | |     // Agent-gate signal: agent.startsWith("hf-")
 240 | |     const sessionFlags = new Map<string, { interrupted: boolean; activeAgentIsHf: boolean }>();
 241 | |   
 242 | |     // LRU access order for eviction (oldest first).
 243 | |     const sessionAccessOrder: string[] = [];
 244 | |   
 245 | |     /**
 246 | |      * Touch a sessionId for LRU tracking and evict the oldest entry if the map
 247 | |      * is at capacity. `sessionFlags` and `planBindings` are evicted alongside.
 248 | |      */
 249 | |     function touchSession(sessionId: string): void {
 250 | |       const existingIdx = sessionAccessOrder.indexOf(sessionId);
 251 | |       if (existingIdx !== -1) {
 252 | |         sessionAccessOrder.splice(existingIdx, 1);
 253 | |       }
 254 | |       sessionAccessOrder.push(sessionId);
 255 | |   
 256 | |       // Evict oldest entries when over capacity.
 257 | |       while (sessionRuntimes.size >= SESSION_MAP_MAX && sessionAccessOrder.length > 0) {
 258 | |         const oldest = sessionAccessOrder.shift()!;
 259 | |         if (oldest !== sessionId) {
 260 | |           sessionRuntimes.delete(oldest);
 261 | |           sessionFlags.delete(oldest);
 262 | |           planBindings.delete(oldest);
 263 | |         }
 264 | |       }
 265 | |     }
 266 | |   
 267 | |     /**
 268 | |      * Returns (or lazily creates) the per-session flags object.
 269 | |      */
 270 | |     function getFlags(sessionId: string): { interrupted: boolean; activeAgentIsHf: boolean } {
 271 | |       let flags = sessionFlags.get(sessionId);
 272 | |       if (!flags) {
 273 | |         flags = { interrupted: false, activeAgentIsHf: false };
 274 | |         sessionFlags.set(sessionId, flags);
 275 | |       }
 276 | |       return flags;
 277 | |     }
 278 | |   
 279 | |     /**
 280 | |      * Returns the HybridLoopRuntime for the given session, or null if no plan
 281 | |      * binding has been registered for this session.
 282 | |      *
 283 | |      * Unlike the legacy single-runtime design, this function does NOT fall back
 284 | |      * to auto-discovery. The session must first be bound via planBindings (set by
 285 | |      * the hf_plan_start tool in M2). Sessions with no plan binding are planless
 286 | |      * and will receive no-op hook behaviour.
 287 | |      */
 288 | |     const getRuntime = (sessionId: string): Promise<HybridLoopRuntime | null> => {
 289 | |       const existing = sessionRuntimes.get(sessionId);
 290 | |       if (existing) {
 291 | |         touchSession(sessionId);
 292 | |         return existing;
 293 | |       }
 294 | |   
 295 | |       const explicitPlanPath = planBindings.get(sessionId);
 296 | |       if (!explicitPlanPath) {
 297 | |         hfLog({ tag: "plugin", msg: "getRuntime: no plan binding — session is planless", data: { sessionId } });
 298 | |         return Promise.resolve(null);
 299 | |       }
 300 | |   
 301 | |       hfLog({ tag: "plugin", msg: "getRuntime: starting hydration", data: { sessionId, explicitPlanPath } });
 302 | |       touchSession(sessionId);
 303 | |       const promise: Promise<HybridLoopRuntime | null> = hydrateRuntime(context, explicitPlanPath).catch((error) => {
 304 | |         if (error instanceof Error && error.message === "Runtime hydration timed out") {
 305 | |           hfLog({ tag: "plugin", msg: "getRuntime: hydration timed out", data: { sessionId } });
 306 | |           return null;
 307 | |         }
 308 | |         sessionRuntimes.delete(sessionId);
 309 | |         const orderIdx = sessionAccessOrder.indexOf(sessionId);
 310 | |         if (orderIdx !== -1) sessionAccessOrder.splice(orderIdx, 1);
 311 | |         throw error;
 312 | |       });
 313 | |       sessionRuntimes.set(sessionId, promise);
 314 | |       return promise;
 315 | |     };
 316 | |   
 317 | |     const hooks: Record<string, OpenCodeHook> = {
 318 | |       "message.updated": async (input?: HookInput) => {
 319 | |         hfLog({ tag: "plugin", msg: "hook: message.updated" });
 320 | |         const info = (input as { info?: Record<string, unknown> } | undefined)?.info;
 321 | |         if (!info) {
 322 | |           hfLog({ tag: "plugin", msg: "message.updated: missing info field — skipping flag update" });
 323 | |           return null;
 324 | |         }
 325 | |   
 326 | |         // Resolve sessionId: prefer info.sessionID (SDK Message field), fall back to context.
 327 | |         const infoSessionId = typeof info.sessionID === "string" && info.sessionID.length > 0
 328 | |           ? info.sessionID
 329 | |           : undefined;
 330 | |         const sessionId = infoSessionId ?? context.session?.id;
 331 | |   
 332 | |         if (!sessionId) {
 333 | |           hfLog({ tag: "plugin", msg: "message.updated: no sessionId — skipping flag update" });
 334 | |           return null;
 335 | |         }
 336 | |   
 337 | |         const flags = getFlags(sessionId);
 338 | |         const agent = typeof info.agent === "string" ? info.agent : "";
 339 | |         flags.activeAgentIsHf = agent.startsWith("hf-");
 340 | |         if (info.role === "assistant") {
 341 | |           const error = info.error as { name?: string } | undefined;
 342 | |           if (error?.name === "MessageAbortedError") {
 343 | |             flags.interrupted = true;
 344 | |             hfLog({ tag: "plugin", msg: "message.updated: MessageAbortedError detected — interrupt flagged", data: { sessionId } });
 345 | |           }
 346 | |         }
 347 | |         hfLog({ tag: "plugin", msg: "message.updated: flags updated", data: { sessionId, activeAgentIsHf: flags.activeAgentIsHf, interrupted: flags.interrupted } });
 348 | |         return null;
 349 | |       },
 350 | |       "tool.execute.before": async (input?: HookInput, output?: HookInput) => {
 351 | |         hfLog({ tag: "plugin", msg: "hook: tool.execute.before" });
 352 | |         const command = extractCommand(input, output);
 353 | |         if (isDestructiveCommand(command)) {
 354 | |           throw new Error("Hybrid runtime guardrail blocked a destructive command.");
 355 | |         }
 356 | |         // Fire-and-forget: event recording must not block tool execution
 357 | |         const sessionId = extractSessionId(input, output, context);
 358 | |         if (sessionId) {
 359 | |           getRuntime(sessionId).then((runtime) => {
 360 | |             if (runtime) {
 361 | |               recordOpenCodeEvent(runtime, "tool.execute.before", input, output, context).catch(() => { });
 362 | |             }
 363 | |           }).catch(() => { });
 364 | |         }
 365 | |         return null;
 366 | |       },
 367 | |       "session.created": async (input?: HookInput, output?: HookInput) => {
 368 | |         return withHookDeadline((async () => {
 369 | |           const done = hfLogTimed({ tag: "plugin", msg: "hook: session.created" });
 370 | |           const sessionId = extractSessionId(input, output, context);
 371 | |           if (!sessionId) {
 372 | |             hfLog({ tag: "plugin", msg: "session.created: no sessionId — skipping" });
 373 | |             return {};
 374 | |           }
 375 | |           const runtime = await getRuntime(sessionId);
 376 | |           if (!runtime) {
 377 | |             hfLog({ tag: "plugin", msg: "session.created: no runtime (no plan binding)", data: { sessionId } });
 378 | |             done({ skipped: "no-binding" });
 379 | |             return {};
 380 | |           }
 381 | |           await recordOpenCodeEvent(runtime, "session.created", input, output, context);
 382 | |           const decision = await runtime.decideNext();
 383 | |           done({ action: decision.action });
 384 | |           return { additionalContext: decision.resume_prompt };
 385 | |         })(), {});
 386 | |       },
 387 | |       "session.status": async () => {
 388 | |         // session.status is not per-session in the original design — it was a single runtime.
 389 | |         // After M1, this hook cannot return meaningful per-session data without a sessionId.
 390 | |         // Return a disabled status to indicate no active plan.
 391 | |         return { enabled: false, reason: "no_active_session" };
 392 | |       },
 393 | |       "session.compacted": async (input?: HookInput, output?: HookInput) => {
 394 | |         return withHookDeadline((async () => {
 395 | |           const done = hfLogTimed({ tag: "plugin", msg: "hook: session.compacted" });
 396 | |           const sessionId = extractSessionId(input, output, context);
 397 | |           if (!sessionId) {
 398 | |             hfLog({ tag: "plugin", msg: "session.compacted: no sessionId — skipping" });
 399 | |             done({ skipped: true });
 400 | |             return {};
 401 | |           }
 402 | |           const flags = getFlags(sessionId);
 403 | |           if (!flags.activeAgentIsHf) {
 404 | |             hfLog({ tag: "plugin", msg: "session.compacted: non-hf agent — skipping", data: { sessionId } });
 405 | |             done({ skipped: true });
 406 | |             return {};
 407 | |           }
 408 | |           const runtime = await getRuntime(sessionId);
 409 | |           if (!runtime) {
 410 | |             hfLog({ tag: "plugin", msg: "session.compacted: no runtime (no plan binding)", data: { sessionId } });
 411 | |             return {};
 412 | |           }
 413 | |           await recordOpenCodeEvent(runtime, "session.compacted", input, output, context);
 414 | |           await recordCompactionArchive(runtime, "opencode", "opencode.pre_compact_archive");
 415 | |           const decision = await runtime.decideNext();
 416 | |           done({ action: decision.action });
 417 | |           return { additionalContext: decision.resume_prompt };
 418 | |         })(), {});
 419 | |       },
 420 | |       "session.idle": async (input?: HookInput, output?: HookInput) => {
 421 | |         return withHookDeadline((async () => {
 422 | |           const done = hfLogTimed({ tag: "plugin", msg: "hook: session.idle" });
 423 | |           const sessionId = extractSessionId(input, output, context);
 424 | |           if (!sessionId) {
 425 | |             hfLog({ tag: "plugin", msg: "session.idle: no sessionId — skipping" });
 426 | |             done({ skipped: "no-session" });
 427 | |             return null;
 428 | |           }
 429 | |   
 430 | |           const flags = getFlags(sessionId);
 431 | |   
 432 | |           // Guard 1: ESC interrupt — user aborted generation, never auto-continue.
 433 | |           if (flags.interrupted) {
 434 | |             flags.interrupted = false;
 435 | |             hfLog({ tag: "plugin", msg: "session.idle: interrupted — skipping auto-continue", data: { sessionId } });
 436 | |             done({ skipped: "interrupted" });
 437 | |             return { action: "allow_stop", reason: "User interrupted generation (ESC). Skipping auto-continue." };
 438 | |           }
 439 | |   
 440 | |           // Guard 2: agent gate — only run runtime logic for hf-* agents.
 441 | |           if (!flags.activeAgentIsHf) {
 442 | |             hfLog({ tag: "plugin", msg: "session.idle: non-hf agent — skipping", data: { sessionId } });
 443 | |             done({ skipped: "non-hf" });
 444 | |             return null;
 445 | |           }
 446 | |   
 447 | |           const runtime = await getRuntime(sessionId);
 448 | |           if (!runtime) {
 449 | |             hfLog({ tag: "plugin", msg: "session.idle: no runtime (no plan binding)", data: { sessionId } });
 450 | |             return null;
 451 | |           }
 452 | |   
 453 | |           if (runtime.isPlanless()) {
 454 | |             await recordOpenCodeEvent(runtime, "session.idle", input, output, context);
 455 | |             const decision = await runtime.decideNext();
 456 | |             done({ action: decision.action, planless: true });
 457 | |             return decision;
 458 | |           }
 459 | |   
 460 | |           await ingestOpenCodeOutcome(runtime, input, output, sessionId);
 461 | |           const decision = await runtime.decideNext();
 462 | |           const status = runtime.getStatus();
 463 | |   
 464 | |           done({ action: decision.action, loopState: status.loopState });
 465 | |           return applyOpenCodeDecision(decision, {
 466 | |             autoContinue: status.autoContinue,
 467 | |             deliverPrompt: async (prompt) => promptContinuation({ ...decision, resume_prompt: prompt }, context, sessionId)
 468 | |           });
 469 | |         })(), null);
 470 | |       },
 471 | |       "subagent.started": async (input?: HookInput, output?: HookInput) => {
 472 | |         return withHookDeadline((async () => {
 473 | |           const sessionId = extractSessionId(input, output, context);
 474 | |           if (!sessionId) {
 475 | |             return null;
 476 | |           }
 477 | |           const flags = getFlags(sessionId);
 478 | |           if (!flags.activeAgentIsHf) {
 479 | |             hfLog({ tag: "plugin", msg: "subagent.started: non-hf agent — skipping", data: { sessionId } });
 480 | |             return null;
 481 | |           }
 482 | |           const runtime = await getRuntime(sessionId);
 483 | |           if (!runtime) {
 484 | |             return null;
 485 | |           }
 486 | |           await recordOpenCodeEvent(runtime, "subagent.started", input, output, context);
 487 | |           const subagentId = String(input?.subagent_id ?? input?.id ?? "unknown");
 488 | |           const subagentName = String(input?.subagent_name ?? input?.name ?? "unnamed");
 489 | |           await recordSubagentLifecycle(runtime, {
 490 | |             id: subagentId,
 491 | |             name: subagentName,
 492 | |             status: "running"
 493 | |           });
 494 | |           return null;
 495 | |         })(), null);
 496 | |       },
 497 | |       "subagent.completed": async (input?: HookInput, output?: HookInput) => {
 498 | |         return withHookDeadline((async () => {
 499 | |           const sessionId = extractSessionId(input, output, context);
 500 | |           if (!sessionId) {
 501 | |             return null;
 502 | |           }
 503 | |           const flags = getFlags(sessionId);
 504 | |           if (!flags.activeAgentIsHf) {
 505 | |             hfLog({ tag: "plugin", msg: "subagent.completed: non-hf agent — skipping", data: { sessionId } });
 506 | |             return null;
 507 | |           }
 508 | |           const runtime = await getRuntime(sessionId);
 509 | |           if (!runtime) {
 510 | |             return null;
 511 | |           }
 512 | |           await recordOpenCodeEvent(runtime, "subagent.completed", input, output, context);
 513 | |           const subagentId = String(input?.subagent_id ?? input?.id ?? "unknown");
 514 | |           const subagentName = String(input?.subagent_name ?? input?.name ?? "unnamed");
 515 | |           const failed = input?.error !== undefined;
 516 | |           await recordSubagentLifecycle(runtime, {
 517 | |             id: subagentId,
 518 | |             name: subagentName,
 519 | |             status: failed ? "failed" : "completed"
 520 | |           });
 521 | |           return null;
 522 | |         })(), null);
 523 | |       }
 524 | |     };
 525 | |   
 526 | |     /**
 527 | |      * `hf_plan_start` tool: binds the current session to a plan doc.
 528 | |      *
 529 | |      * Agents call this at the start of a session (or mid-session to re-bind).
 530 | |      * The tool resolves the plan slug/path, stores the binding, evicts any
 531 | |      * existing runtime, eagerly hydrates a fresh runtime, and returns a
 532 | |      * status summary so the agent can proceed immediately.
 533 | |      */
 534 | |     const hfPlanStartTool = {
 535 | |       description: "Bind this OpenCode session to a specific hybrid-framework plan doc. Call this at the start of every builder or planner session before running any plan milestones. Pass a plan slug (e.g. 'my-feature') or a relative path (e.g. 'plans/2026-03-27-my-feature-plan.md').",
 536 | |       args: {
 537 | |         plan: pluginZ.string().describe("Plan slug or relative path (e.g. 'my-feature' or 'plans/2026-03-27-my-feature-plan.md')")
 538 | |       },
 539 | |       execute: async (args: { plan: string }, toolContext: ToolContext): Promise<string> => {
 540 | |         const planArg = args.plan ?? "";
 541 | |         const baseDir = toolContext.directory;
 542 | |   
 543 | |         // Resolve the plan path.
 544 | |         let resolvedPath: string;
 545 | |   
 546 | |         if (planArg.includes("/") || planArg.includes("\\") || planArg.endsWith(".md")) {
 547 | |           // Looks like an explicit relative path — resolve directly.
 548 | |           resolvedPath = path.resolve(baseDir, planArg);
 549 | |           try {
 550 | |             await fs.access(resolvedPath);
 551 | |           } catch {
 552 | |             // Scan for available plans to include in error.
 553 | |             const available = await listAvailablePlans(baseDir);
 554 | |             const listStr = available.length > 0
 555 | |               ? available.map((p) => `  - ${p}`).join("\n")
 556 | |               : "  (no plans found)";
 557 | |             return `Error: Plan file not found: ${planArg}\n\nAvailable plans:\n${listStr}`;
 558 | |           }
 559 | |         } else {
 560 | |           // Bare slug — scan plans/ directory for matching files.
 561 | |           const plansDir = path.join(baseDir, "plans");
 562 | |           let entries: string[] = [];
 563 | |           try {
 564 | |             const raw = await fs.readdir(plansDir);
 565 | |             entries = raw;
 566 | |           } catch {
 567 | |             return `Error: Could not read plans/ directory at ${plansDir}. Make sure you are running from the project root.`;
 568 | |           }
 569 | |   
 570 | |           const candidates = entries.filter((name) => {
 571 | |             const lower = name.toLowerCase();
 572 | |             return lower.includes(planArg.toLowerCase()) && lower.endsWith("-plan.md");
 573 | |           });
 574 | |   
 575 | |           if (candidates.length === 0) {
 576 | |             const allPlans = entries.filter((name) => name.endsWith("-plan.md"));
 577 | |             const listStr = allPlans.length > 0
 578 | |               ? allPlans.map((p) => `  - plans/${p}`).join("\n")
 579 | |               : "  (no plan files found in plans/)";
 580 | |             return `Error: No plan found matching "${planArg}".\n\nAvailable plans:\n${listStr}`;
 581 | |           }
 582 | |   
 583 | |           if (candidates.length > 1) {
 584 | |             const listStr = candidates.map((p) => `  - plans/${p}`).join("\n");
 585 | |             return `Error: Ambiguous plan slug "${planArg}" — matches ${candidates.length} files. Be more specific:\n${listStr}`;
 586 | |           }
 587 | |   
 588 | |           resolvedPath = path.join(plansDir, candidates[0]!);
 589 | |         }
 590 | |   
 591 | |         const sessionId = toolContext.sessionID;
 592 | |   
 593 | |         // Store the binding — getRuntime() will use this on next call.
 594 | |         planBindings.set(sessionId, resolvedPath);
 595 | |   
 596 | |         // Evict any existing runtime for this session (forces re-hydration).
 597 | |         sessionRuntimes.delete(sessionId);
 598 | |         const orderIdx = sessionAccessOrder.indexOf(sessionId);
 599 | |         if (orderIdx !== -1) sessionAccessOrder.splice(orderIdx, 1);
 600 | |   
 601 | |         hfLog({ tag: "plugin", msg: "hf_plan_start: plan bound", data: { sessionId, resolvedPath } });
 602 | |   
 603 | |         // Eagerly hydrate so the first hook call doesn't pay the cost.
 604 | |         let summary: string;
 605 | |         try {
 606 | |           const runtime = await getRuntime(sessionId);
 607 | |           if (runtime && !runtime.isPlanless()) {
 608 | |             const status = runtime.getStatus();
 609 | |             // Parse plan doc for richer info (title, milestone count).
 610 | |             try {
 611 | |               const parsed = await parsePlan(resolvedPath);
 612 | |               const planTitle = parsed.milestones.length > 0
 613 | |                 ? (parsed.userIntent ? parsed.userIntent.split("\n")[0]?.trim() ?? path.basename(resolvedPath) : path.basename(resolvedPath))
 614 | |                 : path.basename(resolvedPath);
 615 | |               const totalMilestones = parsed.milestones.length;
 616 | |               const currentIdx = parsed.currentMilestone?.index ?? null;
 617 | |               const currentTitle = parsed.currentMilestone?.title ?? "(all complete)";
 618 | |               summary = [
 619 | |                 `Plan bound: ${resolvedPath}`,
 620 | |                 `Title: ${planTitle}`,
 621 | |                 `Status: ${parsed.status}`,
 622 | |                 `Loop state: ${status.loopState}`,
 623 | |                 `Current milestone: ${currentIdx !== null ? `M${currentIdx} — ${currentTitle}` : "(all complete)"}`,
 624 | |                 `Total milestones: ${totalMilestones} (${parsed.milestones.filter((m) => m.checked).length} complete)`
 625 | |               ].join("\n");
 626 | |             } catch {
 627 | |               summary = [
 628 | |                 `Plan bound: ${resolvedPath}`,
 629 | |                 `Loop state: ${status.loopState}`
 630 | |               ].join("\n");
 631 | |             }
 632 | |           } else {
 633 | |             summary = `Plan bound: ${resolvedPath}\n(Runtime hydration returned planless or null — check plan doc format.)`;
 634 | |           }
 635 | |         } catch (err) {
 636 | |           summary = `Plan bound: ${resolvedPath}\n(Warning: runtime hydration failed: ${String(err)})`;
 637 | |         }
 638 | |   
 639 | |         return summary;
 640 | |       }
 641 | |     };
 642 | |   
 643 | |     /**
 644 | |      * `hf_search` tool: performs an ad-hoc semantic search against the
 645 | |      * project's unified vector index (vault markdown + TypeScript source code).
 646 | |      *
 647 | |      * Agents call this when the context dispatched to them is insufficient or
 648 | |      * when they need to explore conventions, patterns, or related implementations
 649 | |      * in the codebase. Optionally filter by source type.
 650 | |      */
 651 | |     const hfSearchTool = {
 652 | |       description: "Search the hybrid framework's unified semantic index (vault docs, code, and external files). Optionally filter by source: 'vault' for documentation, 'code' for source code, 'all' (default) for everything.",
 653 | |       args: {
 654 | |         query: pluginZ.string().describe("Natural language search query"),
 655 | |         top_k: pluginZ.number().describe("Number of results to return (default: 5)").optional(),
 656 | |         source: pluginZ.string().describe("Source filter: 'vault' for documentation only, 'code' for source code only, 'all' for everything (default: 'all')").optional()
 657 | |       },
 658 | |       execute: async (args: { query: string; top_k?: number; source?: string }, toolContext: ToolContext): Promise<string> => {
 659 | |         try {
 660 | |           const query = args.query ?? "";
 661 | |           const topK = args.top_k !== undefined ? args.top_k : undefined;
 662 | |           const sourceArg = args.source;
 663 | |           const sourceFilter: "vault" | "code" | undefined =
 664 | |             sourceArg === "vault" ? "vault"
 665 | |             : sourceArg === "code" ? "code"
 666 | |             : undefined;
 667 | |   
 668 | |           let runtime = await getRuntime(toolContext.sessionID);
 669 | |   
 670 | |           // No plan binding for this session — spin up a disposable planless runtime
 671 | |           // so the tool can still search the code index even without an active plan.
 672 | |           if (!runtime) {
 673 | |             try {
 674 | |               const planlessRuntime = await hydrateRuntimeWithTimeout({
 675 | |                 cwd: context.cwd ?? toolContext.directory,
 676 | |                 timeoutMs: 15_000,
 677 | |                 timeoutMessage: "Runtime hydration timed out",
 678 | |                 tag: "plugin/hf_search",
 679 | |               });
 680 | |               runtime = planlessRuntime;
 681 | |             } catch {
 682 | |               return "No index available — the unified index has not been built yet for this session.";
 683 | |             }
 684 | |           }
 685 | |   
 686 | |           const results = await runtime.queryIndex(query, topK, sourceFilter);
 687 | |           if (results === null) {
 688 | |             return "No index available — the unified index has not been built yet for this session.";
 689 | |           }
 690 | |   
 691 | |           return formatToolSearchResults(results);
 692 | |         } catch (err) {
 693 | |           return `Error querying index: ${String(err)}`;
 694 | |         }
 695 | |       }
 696 | |     };
 697 | |   
 698 | |     const tools: Record<string, unknown> = {
 699 | |       hf_plan_start: hfPlanStartTool,
 700 | |       hf_search: hfSearchTool
 701 | |     };
 702 | |   
 703 | |     return { hooks, tools, planBindings, sessionRuntimes, getRuntime };
 704 | `-> }
 705 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines-per-function): The async function execute has too many lines (82). Maximum allowed is 50.
     ,-[src/opencode/plugin.ts:539:14]
 538 |         },
 539 | ,->     execute: async (args: { plan: string }, toolContext: ToolContext): Promise<string> => {
 540 | |         const planArg = args.plan ?? "";
 541 | |         const baseDir = toolContext.directory;
 542 | |   
 543 | |         // Resolve the plan path.
 544 | |         let resolvedPath: string;
 545 | |   
 546 | |         if (planArg.includes("/") || planArg.includes("\\") || planArg.endsWith(".md")) {
 547 | |           // Looks like an explicit relative path — resolve directly.
 548 | |           resolvedPath = path.resolve(baseDir, planArg);
 549 | |           try {
 550 | |             await fs.access(resolvedPath);
 551 | |           } catch {
 552 | |             // Scan for available plans to include in error.
 553 | |             const available = await listAvailablePlans(baseDir);
 554 | |             const listStr = available.length > 0
 555 | |               ? available.map((p) => `  - ${p}`).join("\n")
 556 | |               : "  (no plans found)";
 557 | |             return `Error: Plan file not found: ${planArg}\n\nAvailable plans:\n${listStr}`;
 558 | |           }
 559 | |         } else {
 560 | |           // Bare slug — scan plans/ directory for matching files.
 561 | |           const plansDir = path.join(baseDir, "plans");
 562 | |           let entries: string[] = [];
 563 | |           try {
 564 | |             const raw = await fs.readdir(plansDir);
 565 | |             entries = raw;
 566 | |           } catch {
 567 | |             return `Error: Could not read plans/ directory at ${plansDir}. Make sure you are running from the project root.`;
 568 | |           }
 569 | |   
 570 | |           const candidates = entries.filter((name) => {
 571 | |             const lower = name.toLowerCase();
 572 | |             return lower.includes(planArg.toLowerCase()) && lower.endsWith("-plan.md");
 573 | |           });
 574 | |   
 575 | |           if (candidates.length === 0) {
 576 | |             const allPlans = entries.filter((name) => name.endsWith("-plan.md"));
 577 | |             const listStr = allPlans.length > 0
 578 | |               ? allPlans.map((p) => `  - plans/${p}`).join("\n")
 579 | |               : "  (no plan files found in plans/)";
 580 | |             return `Error: No plan found matching "${planArg}".\n\nAvailable plans:\n${listStr}`;
 581 | |           }
 582 | |   
 583 | |           if (candidates.length > 1) {
 584 | |             const listStr = candidates.map((p) => `  - plans/${p}`).join("\n");
 585 | |             return `Error: Ambiguous plan slug "${planArg}" — matches ${candidates.length} files. Be more specific:\n${listStr}`;
 586 | |           }
 587 | |   
 588 | |           resolvedPath = path.join(plansDir, candidates[0]!);
 589 | |         }
 590 | |   
 591 | |         const sessionId = toolContext.sessionID;
 592 | |   
 593 | |         // Store the binding — getRuntime() will use this on next call.
 594 | |         planBindings.set(sessionId, resolvedPath);
 595 | |   
 596 | |         // Evict any existing runtime for this session (forces re-hydration).
 597 | |         sessionRuntimes.delete(sessionId);
 598 | |         const orderIdx = sessionAccessOrder.indexOf(sessionId);
 599 | |         if (orderIdx !== -1) sessionAccessOrder.splice(orderIdx, 1);
 600 | |   
 601 | |         hfLog({ tag: "plugin", msg: "hf_plan_start: plan bound", data: { sessionId, resolvedPath } });
 602 | |   
 603 | |         // Eagerly hydrate so the first hook call doesn't pay the cost.
 604 | |         let summary: string;
 605 | |         try {
 606 | |           const runtime = await getRuntime(sessionId);
 607 | |           if (runtime && !runtime.isPlanless()) {
 608 | |             const status = runtime.getStatus();
 609 | |             // Parse plan doc for richer info (title, milestone count).
 610 | |             try {
 611 | |               const parsed = await parsePlan(resolvedPath);
 612 | |               const planTitle = parsed.milestones.length > 0
 613 | |                 ? (parsed.userIntent ? parsed.userIntent.split("\n")[0]?.trim() ?? path.basename(resolvedPath) : path.basename(resolvedPath))
 614 | |                 : path.basename(resolvedPath);
 615 | |               const totalMilestones = parsed.milestones.length;
 616 | |               const currentIdx = parsed.currentMilestone?.index ?? null;
 617 | |               const currentTitle = parsed.currentMilestone?.title ?? "(all complete)";
 618 | |               summary = [
 619 | |                 `Plan bound: ${resolvedPath}`,
 620 | |                 `Title: ${planTitle}`,
 621 | |                 `Status: ${parsed.status}`,
 622 | |                 `Loop state: ${status.loopState}`,
 623 | |                 `Current milestone: ${currentIdx !== null ? `M${currentIdx} — ${currentTitle}` : "(all complete)"}`,
 624 | |                 `Total milestones: ${totalMilestones} (${parsed.milestones.filter((m) => m.checked).length} complete)`
 625 | |               ].join("\n");
 626 | |             } catch {
 627 | |               summary = [
 628 | |                 `Plan bound: ${resolvedPath}`,
 629 | |                 `Loop state: ${status.loopState}`
 630 | |               ].join("\n");
 631 | |             }
 632 | |           } else {
 633 | |             summary = `Plan bound: ${resolvedPath}\n(Runtime hydration returned planless or null — check plan doc format.)`;
 634 | |           }
 635 | |         } catch (err) {
 636 | |           summary = `Plan bound: ${resolvedPath}\n(Warning: runtime hydration failed: ${String(err)})`;
 637 | |         }
 638 | |   
 639 | |         return summary;
 640 | `->     }
 641 |       };
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines-per-function): The function toOpenCodeHooks has too many lines (63). Maximum allowed is 50.
     ,-[src/opencode/plugin.ts:735:1]
 734 |      */
 735 | ,-> function toOpenCodeHooks(
 736 | |     internalHooks: Record<string, OpenCodeHook>,
 737 | |     tools: Record<string, unknown>,
 738 | |     context: OpenCodePluginContext
 739 | |   ): Record<string, unknown> {
 740 | |     return {
 741 | |       /* ── filter hook – unchanged ─────────────────────────────────── */
 742 | |       "tool.execute.before": internalHooks["tool.execute.before"],
 743 | |   
 744 | |       /* ── plugin-registered tools ────────────────────────────────── */
 745 | |       tool: tools,
 746 | |   
 747 | |       /* ── compaction hook – inject runtime context before compaction  */
 748 | |       "experimental.session.compacting": async (
 749 | |         input: { sessionID: string },
 750 | |         output: { context: string[]; prompt?: string }
 751 | |       ) => {
 752 | |         const done = hfLogTimed({ tag: "plugin", msg: "hook: experimental.session.compacting" });
 753 | |         try {
 754 | |           const result = await withHookDeadline(
 755 | |             (async () => {
 756 | |               return internalHooks["session.compacted"]?.({ sessionID: input.sessionID });
 757 | |             })(),
 758 | |             undefined
 759 | |           );
 760 | |           const additionalContext = (result as { additionalContext?: string } | undefined)?.additionalContext;
 761 | |           if (additionalContext) {
 762 | |             output.context.push(additionalContext);
 763 | |           }
 764 | |           done({ injected: !!additionalContext });
 765 | |         } catch {
 766 | |           done({ error: true });
 767 | |         }
 768 | |       },
 769 | |   
 770 | |       /* ── event observer – dispatches lifecycle events to internal hooks  */
 771 | |       event: async (eventInput: { event: { type: string; properties?: Record<string, unknown> } }) => {
 772 | |         const { event } = eventInput;
 773 | |         hfLog({ tag: "plugin", msg: `event: ${event.type}`, ...(event.properties ? { data: event.properties } : {}) });
 774 | |   
 775 | |         try {
 776 | |           const props = (event.properties ?? {}) as HookInput;
 777 | |   
 778 | |           switch (event.type) {
 779 | |             case "message.updated": {
 780 | |               // props.info is the Message object (UserMessage | AssistantMessage).
 781 | |               // The internal handler reads agent + error fields to update per-session flags.
 782 | |               const info = event.properties?.info;
 783 | |               await internalHooks["message.updated"]?.({ info });
 784 | |               break;
 785 | |             }
 786 | |   
 787 | |             case "session.created":
 788 | |               await internalHooks["session.created"]?.(props);
 789 | |               break;
 790 | |   
 791 | |             case "session.idle":
 792 | |               await internalHooks["session.idle"]?.(props);
 793 | |               break;
 794 | |   
 795 | |             case "session.compacted":
 796 | |               // Post-compaction notification – context injection already
 797 | |               // handled by experimental.session.compacting above.
 798 | |               break;
 799 | |   
 800 | |             case "session.status":
 801 | |               // Informational only – can't return data from event hook.
 802 | |               break;
 803 | |   
 804 | |             default:
 805 | |               // Catch subagent and other events.
 806 | |               if (event.type.startsWith("subagent.")) {
 807 | |                 const hookName = event.type as string;
 808 | |                 await internalHooks[hookName]?.(props);
 809 | |               }
 810 | |               break;
 811 | |           }
 812 | |         } catch (err) {
 813 | |           hfLog({ tag: "plugin", msg: `event handler error: ${event.type}`, data: { error: String(err) } });
 814 | |         }
 815 | |       }
 816 | |     };
 817 | `-> }
 818 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines): File has too many lines (657).
     ,-[src/opencode/plugin.ts:858:36]
 857 | 
 858 | export default HybridRuntimePlugin;
     :                                     ^
     `----
  help: Maximum allowed is 500.

  x eslint(max-params): Function 'recordOpenCodeEvent' has too many parameters (5). Maximum allowed is 3.
     ,-[src/opencode/plugin.ts:140:35]
 139 |     
 140 | ,-> async function recordOpenCodeEvent(
 141 | |     runtime: HybridLoopRuntime,
 142 | |     eventType: string,
 143 | |     input: HookInput,
 144 | |     output: HookInput,
 145 | |     context: OpenCodePluginContext
 146 | `-> ): Promise<HybridLoopRuntime> {
 147 |       const sessionId = extractSessionId(input, output, context);
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(max-params): Function 'ingestOpenCodeOutcome' has too many parameters (4). Maximum allowed is 3.
     ,-[src/opencode/plugin.ts:159:37]
 158 | 
 159 | async function ingestOpenCodeOutcome(runtime: HybridLoopRuntime, input: HookInput, output: HookInput, sessionId?: string): Promise<{
     :                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 160 |   ingested: boolean;
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(no-nested-ternary): Do not nest ternary expressions.
     ,-[src/opencode/plugin.ts:612:31]
 611 |                 const parsed = await parsePlan(resolvedPath);
 612 | ,->             const planTitle = parsed.milestones.length > 0
 613 | |                 ? (parsed.userIntent ? parsed.userIntent.split("\n")[0]?.trim() ?? path.basename(resolvedPath) : path.basename(resolvedPath))
 614 | `->               : path.basename(resolvedPath);
 615 |                 const totalMilestones = parsed.milestones.length;
     `----

  x eslint(no-nested-ternary): Do not nest ternary expressions.
     ,-[src/opencode/plugin.ts:664:11]
 663 |             const sourceFilter: "vault" | "code" | undefined =
 664 | ,->           sourceArg === "vault" ? "vault"
 665 | |             : sourceArg === "code" ? "code"
 666 | `->           : undefined;
 667 |     
     `----

  x eslint(no-unused-vars): Parameter 'context' is declared but never used. Unused parameters should start with a '_'.
     ,-[src/opencode/plugin.ts:738:3]
 737 |   tools: Record<string, unknown>,
 738 |   context: OpenCodePluginContext
     :   ^^^|^^^
     :      `-- 'context' is declared here
 739 | ): Record<string, unknown> {
     `----
  help: Consider removing this parameter.

  x typescript-eslint(consistent-type-imports): All imports in the declaration are only used as types. Use `import type`.
    ,-[src/opencode/plugin.ts:12:1]
 11 | } from "../adapters/lifecycle.js";
 12 | import { HybridLoopRuntime } from "../runtime/runtime.js";
    : ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 13 | import { parsePlan } from "../runtime/plan-doc.js";
    `----

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/opencode/plugin.ts:121:71]
 120 |   });
 121 |   return Promise.race([promise, deadline]).finally(() => clearTimeout(timer!));
     :                                                                       ^^^^^^
 122 | }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/opencode/plugin.ts:258:22]
 257 |     while (sessionRuntimes.size >= SESSION_MAP_MAX && sessionAccessOrder.length > 0) {
 258 |       const oldest = sessionAccessOrder.shift()!;
     :                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^
 259 |       if (oldest !== sessionId) {
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/opencode/plugin.ts:588:44]
 587 | 
 588 |         resolvedPath = path.join(plansDir, candidates[0]!);
     :                                            ^^^^^^^^^^^^^^
 589 |       }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint-plugin-unicorn(no-useless-fallback-in-spread): Empty fallbacks in spreads are unnecessary
     ,-[src/opencode/plugin.ts:107:5]
 106 |   return {
 107 |     ...(input ?? {}),
     :     ^^^^^^^^^^^^^^^^
 108 |     ...(output ? { output } : {})
     `----
  help: Spreading falsy values in object literals won't add any unexpected properties, so it's unnecessary to add an empty object as fallback.

  x eslint(max-lines-per-function): The function validateRuntimeStatus has too many lines (59). Maximum allowed is 50.
     ,-[src/runtime/persistence.ts:313:1]
 312 |     
 313 | ,-> function validateRuntimeStatus(value: unknown, statusPath: string): asserts value is RuntimeStatus {
 314 | |     const p = statusPath;
 315 | |     expectObject(value, "root", p);
 316 | |   
 317 | |     if (value.version !== 1) fail(p, "version", "1");
 318 | |     expectFields(value, { planPath: "string", planSlug: "string", planMtimeMs: "number", autoContinue: "boolean", updatedAt: "string" }, "", p);
 319 | |     expectEnum(value.loopState, ["idle", "running", "paused", "escalated", "complete"], "loopState", p);
 320 | |     expectEnum(value.phase, ["planning", "execution"], "phase", p);
 321 | |   
 322 | |     // currentMilestone: null | object
 323 | |     if (value.currentMilestone !== null) {
 324 | |       expectObject(value.currentMilestone, "currentMilestone", p);
 325 | |       expectFields(value.currentMilestone, { index: "number", checked: "boolean", text: "string", title: "string" }, "currentMilestone", p);
 326 | |     }
 327 | |   
 328 | |     // counters
 329 | |     expectObject(value.counters, "counters", p);
 330 | |     for (const f of ["totalAttempts", "totalTurns", "maxTotalTurns", "noProgress", "repeatedBlocker", "verificationFailures", "turnsSinceLastOutcome"]) {
 331 | |       expectNumber(value.counters[f], `counters.${f}`, p);
 332 | |     }
 333 | |   
 334 | |     // sessions
 335 | |     expectObject(value.sessions, "sessions", p);
 336 | |     for (const [vendor, session] of Object.entries(value.sessions)) {
 337 | |       if (session === undefined) continue;
 338 | |       expectObject(session, `sessions.${vendor}`, p);
 339 | |       expectFields(session, { id: "string", updatedAt: "string" }, `sessions.${vendor}`, p);
 340 | |     }
 341 | |   
 342 | |     // subagents
 343 | |     if (!Array.isArray(value.subagents)) fail(p, "subagents", "an array");
 344 | |     (value.subagents as unknown[]).forEach((sub, i) => {
 345 | |       expectObject(sub, `subagents[${i}]`, p);
 346 | |       expectFields(sub, { id: "string", name: "string", startedAt: "string" }, `subagents[${i}]`, p);
 347 | |       expectEnum(sub.status, ["running", "completed", "failed"], `subagents[${i}].status`, p);
 348 | |       optionalString(sub, "completedAt", `subagents[${i}]`, p);
 349 | |     });
 350 | |   
 351 | |     // optional top-level strings
 352 | |     for (const key of ["lastProgressAt", "recommendedNextAction", "lastTurnEvaluatedAt"] as const) {
 353 | |       optionalString(value, key, "", p);
 354 | |     }
 355 | |     optionalBoolean(value, "awaitingBuilderApproval", "", p);
 356 | |   
 357 | |     // lastBlocker
 358 | |     if (value.lastBlocker !== undefined) {
 359 | |       expectObject(value.lastBlocker, "lastBlocker", p);
 360 | |       expectFields(value.lastBlocker, { signature: "string", message: "string", at: "string" }, "lastBlocker", p);
 361 | |     }
 362 | |   
 363 | |     // lastVerification
 364 | |     if (value.lastVerification !== undefined) {
 365 | |       expectObject(value.lastVerification, "lastVerification", p);
 366 | |       expectEnum(value.lastVerification.status, ["pass", "fail", "unknown"], "lastVerification.status", p);
 367 | |       optionalString(value.lastVerification, "summary", "lastVerification", p);
 368 | |       expectString(value.lastVerification.at, "lastVerification.at", p);
 369 | |     }
 370 | |   
 371 | |     // recovery
 372 | |     if (value.recovery !== undefined) {
 373 | |       expectObject(value.recovery, "recovery", p);
 374 | |       expectEnum(value.recovery.trigger, ["stop", "idle", "compact", "resume"], "recovery.trigger", p);
 375 | |       if (value.recovery.sourceTrigger !== undefined) {
 376 | |         expectEnum(value.recovery.sourceTrigger, ["stop", "idle", "compact"], "recovery.sourceTrigger", p);
 377 | |       }
 378 | |       expectEnum(value.recovery.vendor, ["opencode", "claude", "runtime"], "recovery.vendor", p);
 379 | |       expectFields(value.recovery, { eventType: "string", pendingOutcome: "boolean", at: "string" }, "recovery", p);
 380 | |       optionalString(value.recovery, "sessionId", "recovery", p);
 381 | |     }
 382 | |   
 383 | |     // lastOutcome
 384 | |     if (value.lastOutcome !== undefined && value.lastOutcome !== null) {
 385 | |       const issues = validateTurnOutcome(value.lastOutcome);
 386 | |       if (issues.length > 0) {
 387 | |         throw new Error(`Invalid runtime status at ${p}: lastOutcome ${issues[0]?.path} ${issues[0]?.message}`);
 388 | |       }
 389 | |     }
 390 | `-> }
 391 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-params): Function 'expectEnum' has too many parameters (4). Maximum allowed is 3.
     ,-[src/runtime/persistence.ts:281:20]
 280 | 
 281 | function expectEnum(v: unknown, allowed: readonly string[], f: string, p: string): void {
     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 282 |   if (typeof v !== "string" || !allowed.includes(v)) fail(p, f, `one of ${allowed.join(", ")}`);
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(max-params): Function 'expectFields' has too many parameters (4). Maximum allowed is 3.
     ,-[src/runtime/persistence.ts:290:22]
 289 |     /** Validate a flat record where every listed key must be a certain type. */
 290 | ,-> function expectFields(
 291 | |     obj: Record<string, unknown>,
 292 | |     fields: Record<string, "string" | "number" | "boolean">,
 293 | |     prefix: string,
 294 | |     p: string,
 295 | `-> ): void {
 296 |       for (const [key, type] of Object.entries(fields)) {
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(max-params): Function 'optionalString' has too many parameters (4). Maximum allowed is 3.
     ,-[src/runtime/persistence.ts:305:24]
 304 | /** Validate a field only if it's present (not undefined). */
 305 | function optionalString(obj: Record<string, unknown>, key: string, prefix: string, p: string): void {
     :                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 306 |   if (obj[key] !== undefined) expectString(obj[key], `${prefix}.${key}`, p);
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  x eslint(max-params): Function 'optionalBoolean' has too many parameters (4). Maximum allowed is 3.
     ,-[src/runtime/persistence.ts:309:25]
 308 | 
 309 | function optionalBoolean(obj: Record<string, unknown>, key: string, prefix: string, p: string): void {
     :                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 310 |   if (obj[key] !== undefined) expectBoolean(obj[key], `${prefix}.${key}`, p);
     `----
  help: This rule enforces a maximum number of parameters allowed in function definitions.

  ! eslint(no-console): eslint(no-console): Unexpected console statement.
    ,-[src/runtime/persistence.ts:73:7]
 72 |     if (error instanceof SyntaxError) {
 73 |       console.error(`[loadIndexConfig] Invalid JSON in ${configPath}, using defaults.`);
    :       ^^^^^^^^^^^^^
 74 |       return { ...DEFAULT_INDEX_CONFIG };
    `----
  help: Delete this console statement.

  ! eslint(no-console): eslint(no-console): Unexpected console statement.
     ,-[src/runtime/persistence.ts:211:5]
 210 |   writeQueues.set(key, next.catch((error) => {
 211 |     console.error(`[appendEvent] Failed to append to ${key}: ${(error as Error).message}`);
     :     ^^^^^^^^^^^^^
 212 |   }));
     `----
  help: Delete this console statement.

  x eslint(max-lines-per-function): The public async method evaluateTurn has too many lines (70). Maximum allowed is 50.
     ,-[src/runtime/runtime.ts:376:28]
 375 |     
 376 | ,->   public async evaluateTurn(outcome: TurnOutcome): Promise<RuntimeStatus> {
 377 | |       const done = hfLogTimed({ tag: "runtime", msg: "evaluateTurn", data: { state: outcome.state } });
 378 | |       const status = this.requireStatus();
 379 | |       const previousPhase = status.phase;
 380 | |       const plan = await reParsePlanIfChanged(this.requirePlan());
 381 | |       this.plan = plan;
 382 | |       // Invalidate vault — will be reloaded lazily in decideNext().
 383 | |       this.resetVaultState();
 384 | |       this.promptDirty = true;
 385 | |       const now = nowIso();
 386 | |   
 387 | |       status.planMtimeMs = plan.mtimeMs;
 388 | |       status.phase = plan.approved ? "execution" : "planning";
 389 | |       status.currentMilestone = plan.currentMilestone;
 390 | |       status.awaitingBuilderApproval = previousPhase === "planning" && plan.approved && !plan.completed
 391 | |         ? true
 392 | |         : plan.approved
 393 | |           ? status.awaitingBuilderApproval ?? false
 394 | |           : false;
 395 | |       status.counters.totalAttempts += 1;
 396 | |       status.counters.totalTurns += 1;
 397 | |       status.counters.turnsSinceLastOutcome = 0;
 398 | |       delete status.recovery;
 399 | |       status.lastOutcome = outcome;
 400 | |       status.lastTurnEvaluatedAt = now;
 401 | |       status.recommendedNextAction = outcome.next_action;
 402 | |       status.updatedAt = now;
 403 | |   
 404 | |       if (isProgressState(outcome)) {
 405 | |         status.counters.noProgress = 0;
 406 | |         status.counters.repeatedBlocker = 0;
 407 | |         status.lastProgressAt = now;
 408 | |       } else {
 409 | |         status.counters.noProgress += 1;
 410 | |       }
 411 | |   
 412 | |       const signature = blockerSignature(outcome);
 413 | |       if (signature) {
 414 | |         const isSameBlocker = status.lastBlocker?.signature === signature;
 415 | |         status.counters.repeatedBlocker = isSameBlocker ? status.counters.repeatedBlocker + 1 : 1;
 416 | |         status.lastBlocker = {
 417 | |           signature,
 418 | |           message: outcome.blocker?.message ?? outcome.summary,
 419 | |           at: now
 420 | |         };
 421 | |       } else if (isProgressState(outcome)) {
 422 | |         delete status.lastBlocker;
 423 | |       }
 424 | |   
 425 | |       if (hasVerificationFailure(outcome)) {
 426 | |         status.counters.verificationFailures += 1;
 427 | |         status.lastVerification = {
 428 | |           status: "fail",
 429 | |           summary: outcome.tests_run
 430 | |             .filter((test) => test.result === "fail")
 431 | |             .map((test) => `${test.command}: ${test.summary ?? "failed"}`)
 432 | |             .join("; "),
 433 | |           at: now
 434 | |         };
 435 | |       } else if (outcome.tests_run.length > 0) {
 436 | |         status.lastVerification = {
 437 | |           status: "pass",
 438 | |           summary: outcome.tests_run
 439 | |             .filter((test) => test.result === "pass")
 440 | |             .map((test) => `${test.command}: ${test.summary ?? "passed"}`)
 441 | |             .join("; "),
 442 | |           at: now
 443 | |         };
 444 | |         status.counters.verificationFailures = 0;
 445 | |       }
 446 | |   
 447 | |       applyLoopState(plan, status, outcome);
 448 | |   
 449 | |       await this.writeState();
 450 | |       done({ loopState: status.loopState });
 451 | |       return status;
 452 | `->   }
 453 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines-per-function): The public async method decideNext has too many lines (64). Maximum allowed is 50.
     ,-[src/runtime/runtime.ts:505:26]
 504 |     
 505 | ,->   public async decideNext(): Promise<ContinueDecision> {
 506 | |       const status = this.requireStatus();
 507 | |   
 508 | |       if (this.planlessCwd) {
 509 | |         hfLog({ tag: "runtime", msg: "decideNext planless" });
 510 | |         try {
 511 | |           await this.refreshVaultIndex();
 512 | |         } catch (error) {
 513 | |           hfLog({ tag: "runtime", msg: "vault index failed (planless)", data: { error: (error as Error).message } });
 514 | |           this.resetVaultState();
 515 | |         }
 516 | |   
 517 | |         // Load vault shared context for planless compaction recovery
 518 | |         if (!this.vault) {
 519 | |           try {
 520 | |             const vaultPaths = getPlanlessVaultPaths(this.planlessCwd);
 521 | |             this.vault = await readVaultContext(vaultPaths);
 522 | |           } catch (error) {
 523 | |             hfLog({ tag: "runtime", msg: "vault load failed (planless)", data: { error: (error as Error).message } });
 524 | |             this.vault = null;
 525 | |           }
 526 | |         }
 527 | |   
 528 | |         return {
 529 | |           action: "allow_stop",
 530 | |           reason: "No active plan. The runtime is providing guardrails only.",
 531 | |           resume_prompt: buildPlanlessResumePrompt(this.vault, this.vaultSearchResults, {
 532 | |             planningCharBudget: this.indexConfig?.planningCharBudget,
 533 | |           })
 534 | |         };
 535 | |       }
 536 | |   
 537 | |       const plan = this.requirePlan();
 538 | |   
 539 | |       // Lazy vault loading — only when we actually need a resume prompt
 540 | |       if (!this.vault) {
 541 | |         const vaultDone = hfLogTimed({ tag: "runtime", msg: "lazy vault load" });
 542 | |         try {
 543 | |           this.vault = await readVaultContext(getVaultPaths(plan));
 544 | |           await this.refreshVaultIndex();
 545 | |         } catch (error) {
 546 | |           hfLog({ tag: "runtime", msg: "vault load failed", data: { error: (error as Error).message } });
 547 | |           this.vault = null;
 548 | |           this.resetVaultState();
 549 | |         }
 550 | |         // Persist the enriched resume prompt now that vault is loaded
 551 | |         this.promptDirty = true;
 552 | |         await this.writeState();
 553 | |         vaultDone({ hasVault: this.vault !== null, hasIndex: this.unifiedIndex !== null });
 554 | |       }
 555 | |   
 556 | |       const resume_prompt = buildResumePrompt(plan, status, this.vault, this.vaultSearchResults, {
 557 | |         charBudget: this.indexConfig?.charBudget,
 558 | |         planningCharBudget: this.indexConfig?.planningCharBudget,
 559 | |       });
 560 | |   
 561 | |       if (status.awaitingBuilderApproval) {
 562 | |         return {
 563 | |           action: "allow_stop",
 564 | |           reason: "The plan is approved. Wait for explicit human approval before starting `hf-builder`.",
 565 | |           resume_prompt
 566 | |         };
 567 | |       }
 568 | |   
 569 | |       const decision = computeDecision({
 570 | |         approved: plan.approved,
 571 | |         completed: plan.completed,
 572 | |         currentMilestone: plan.currentMilestone,
 573 | |         milestoneCount: plan.milestones.length,
 574 | |         loopState: status.loopState,
 575 | |         counters: status.counters,
 576 | |         lastOutcome: status.lastOutcome,
 577 | |       });
 578 | |   
 579 | |       return { ...decision, resume_prompt };
 580 | `->   }
 581 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines-per-function): The private async method refreshVaultIndex has too many lines (70). Maximum allowed is 50.
     ,-[src/runtime/runtime.ts:681:34]
 680 |     
 681 | ,->   private async refreshVaultIndex(): Promise<void> {
 682 | |       const plan = this.plan;
 683 | |       const planlessCwd = this.planlessCwd;
 684 | |       const vaultPaths = plan ? getVaultPaths(plan) : null;
 685 | |       const cfg = this.indexConfig;
 686 | |       const status = this.status;
 687 | |   
 688 | |       this.unifiedIndex = null;
 689 | |   
 690 | |       if (cfg && !cfg.enabled) {
 691 | |         if (status) delete status.lastIndexError;
 692 | |         return;
 693 | |       }
 694 | |   
 695 | |       try {
 696 | |         this.unifiedIndex = await withTimeout(
 697 | |           buildUnifiedIndex({
 698 | |             repoRoot: getRepoRoot(plan ?? undefined, planlessCwd ?? undefined),
 699 | |             ...(vaultPaths && this.vault ? { vaultPaths, vaultContext: this.vault } : {}),
 700 | |             codeConfig: cfg?.code.enabled !== false
 701 | |               ? {
 702 | |                 roots: cfg?.code.roots ?? ["src"],
 703 | |                 extensions: cfg?.code.extensions,
 704 | |                 exclude: cfg?.code.exclude,
 705 | |               }
 706 | |               : undefined,
 707 | |             embeddingBatchSize: cfg?.embeddingBatchSize,
 708 | |             maxChunkChars: cfg?.maxChunkChars,
 709 | |           }),
 710 | |           cfg?.timeoutMs ?? DEFAULT_INDEX_CONFIG.timeoutMs,
 711 | |           null
 712 | |         );
 713 | |       } catch (error) {
 714 | |         const msg = (error as Error).message;
 715 | |         hfLog({ tag: "runtime", msg: "unified index build failed", data: { error: msg } });
 716 | |         if (status) status.lastIndexError = `index build: ${msg}`;
 717 | |         this.unifiedIndex = null;
 718 | |       }
 719 | |   
 720 | |       if (this.unifiedIndex) {
 721 | |         if (status) delete status.lastIndexError;
 722 | |       }
 723 | |   
 724 | |       if (this.unifiedIndex && plan?.currentMilestone) {
 725 | |         try {
 726 | |           this.vaultSearchResults = await withTimeout(
 727 | |             this.retrieveUnifiedChunks(plan.currentMilestone.text),
 728 | |             cfg?.timeoutMs ?? DEFAULT_INDEX_CONFIG.timeoutMs,
 729 | |             null
 730 | |           );
 731 | |         } catch (error) {
 732 | |           const msg = (error as Error).message;
 733 | |           hfLog({ tag: "runtime", msg: "vault search failed", data: { error: msg } });
 734 | |           if (status) status.lastIndexError = `vault search: ${msg}`;
 735 | |           this.vaultSearchResults = null;
 736 | |         }
 737 | |         return;
 738 | |       }
 739 | |   
 740 | |       if (this.unifiedIndex && plan?.status === "planning" && !plan.currentMilestone && plan.userIntent) {
 741 | |         try {
 742 | |           this.vaultSearchResults = await withTimeout(
 743 | |             this.retrieveUnifiedChunks(plan.userIntent, cfg?.planningSemanticTopK ?? 5),
 744 | |             cfg?.timeoutMs ?? DEFAULT_INDEX_CONFIG.timeoutMs,
 745 | |             null
 746 | |           );
 747 | |         } catch (error) {
 748 | |           const msg = (error as Error).message;
 749 | |           hfLog({ tag: "runtime", msg: "vault search failed (planning)", data: { error: msg } });
 750 | |           if (status) status.lastIndexError = `vault search: ${msg}`;
 751 | |           this.vaultSearchResults = null;
 752 | |         }
 753 | |         return;
 754 | |       }
 755 | |   
 756 | |       this.vaultSearchResults = null;
 757 | `->   }
 758 |     
     `----
  help: Consider splitting it into smaller functions.

  x eslint(max-lines): File has too many lines (661).
     ,-[src/runtime/runtime.ts:786:2]
 785 |   );
 786 | }
     :   ^
     `----
  help: Maximum allowed is 500.

  x eslint(no-nested-ternary): Do not nest ternary expressions.
     ,-[src/runtime/runtime.ts:390:38]
 389 |         status.currentMilestone = plan.currentMilestone;
 390 | ,->     status.awaitingBuilderApproval = previousPhase === "planning" && plan.approved && !plan.completed
 391 | |         ? true
 392 | |         : plan.approved
 393 | |           ? status.awaitingBuilderApproval ?? false
 394 | `->         : false;
 395 |         status.counters.totalAttempts += 1;
     `----

  ! eslint(no-magic-numbers): No magic number: 5
     ,-[src/runtime/runtime.ts:659:49]
 658 |       queryVector,
 659 |       topK ?? this.indexConfig?.semanticTopK ?? 5,
     :                                                 ^
 660 |       sourceFilter,
     `----

  ! eslint(no-magic-numbers): No magic number: 5
     ,-[src/runtime/runtime.ts:743:84]
 742 |         this.vaultSearchResults = await withTimeout(
 743 |           this.retrieveUnifiedChunks(plan.userIntent, cfg?.planningSemanticTopK ?? 5),
     :                                                                                    ^
 744 |           cfg?.timeoutMs ?? DEFAULT_INDEX_CONFIG.timeoutMs,
     `----

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:893:14]
 892 |       expect(Array.isArray(results)).toBe(true);
 893 |       expect(results!.length).toBeGreaterThan(0);
     :              ^^^^^^^^
 894 |       // Results should be sorted by score descending (item-1 has higher score)
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:895:14]
 894 |       // Results should be sorted by score descending (item-1 has higher score)
 895 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
     :              ^^^^^^^^^^^^
 896 |       expect(results![0]!.metadata.kind).toBe("vault");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:895:14]
 894 |       // Results should be sorted by score descending (item-1 has higher score)
 895 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
     :              ^^^^^^^^
 896 |       expect(results![0]!.metadata.kind).toBe("vault");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:896:14]
 895 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
 896 |       expect(results![0]!.metadata.kind).toBe("vault");
     :              ^^^^^^^^^^^^
 897 |       expect(results![1]!.metadata.kind).toBe("code");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:896:14]
 895 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
 896 |       expect(results![0]!.metadata.kind).toBe("vault");
     :              ^^^^^^^^
 897 |       expect(results![1]!.metadata.kind).toBe("code");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:897:14]
 896 |       expect(results![0]!.metadata.kind).toBe("vault");
 897 |       expect(results![1]!.metadata.kind).toBe("code");
     :              ^^^^^^^^^^^^
 898 |       // Shape check: required fields present
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:897:14]
 896 |       expect(results![0]!.metadata.kind).toBe("vault");
 897 |       expect(results![1]!.metadata.kind).toBe("code");
     :              ^^^^^^^^
 898 |       // Shape check: required fields present
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:899:21]
 898 |       // Shape check: required fields present
 899 |       expect(typeof results![0]!.score).toBe("number");
     :                     ^^^^^^^^^^^^
 900 |       expect(typeof results![0]!.text).toBe("string");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:899:21]
 898 |       // Shape check: required fields present
 899 |       expect(typeof results![0]!.score).toBe("number");
     :                     ^^^^^^^^
 900 |       expect(typeof results![0]!.text).toBe("string");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:900:21]
 899 |       expect(typeof results![0]!.score).toBe("number");
 900 |       expect(typeof results![0]!.text).toBe("string");
     :                     ^^^^^^^^^^^^
 901 |       expect(typeof results![0]!.metadata.sectionTitle).toBe("string");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:900:21]
 899 |       expect(typeof results![0]!.score).toBe("number");
 900 |       expect(typeof results![0]!.text).toBe("string");
     :                     ^^^^^^^^
 901 |       expect(typeof results![0]!.metadata.sectionTitle).toBe("string");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:901:21]
 900 |       expect(typeof results![0]!.text).toBe("string");
 901 |       expect(typeof results![0]!.metadata.sectionTitle).toBe("string");
     :                     ^^^^^^^^^^^^
 902 |       expect(typeof results![0]!.metadata.documentTitle).toBe("string");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:901:21]
 900 |       expect(typeof results![0]!.text).toBe("string");
 901 |       expect(typeof results![0]!.metadata.sectionTitle).toBe("string");
     :                     ^^^^^^^^
 902 |       expect(typeof results![0]!.metadata.documentTitle).toBe("string");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:902:21]
 901 |       expect(typeof results![0]!.metadata.sectionTitle).toBe("string");
 902 |       expect(typeof results![0]!.metadata.documentTitle).toBe("string");
     :                     ^^^^^^^^^^^^
 903 |     });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:902:21]
 901 |       expect(typeof results![0]!.metadata.sectionTitle).toBe("string");
 902 |       expect(typeof results![0]!.metadata.documentTitle).toBe("string");
     :                     ^^^^^^^^
 903 |     });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:955:14]
 954 |       expect(topK2).not.toBeNull();
 955 |       expect(topK2!.length).toBe(2);
     :              ^^^^^^
 956 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/runtime.test.ts:959:14]
 958 |       expect(topK3).not.toBeNull();
 959 |       expect(topK3!.length).toBe(3);
     :              ^^^^^^
 960 |     });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1017:14]
 1016 |       expect(results).not.toBeNull();
 1017 |       expect(results!.length).toBe(1);
      :              ^^^^^^^^
 1018 |       expect(results![0]!.metadata.kind).toBe("vault");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1018:14]
 1017 |       expect(results!.length).toBe(1);
 1018 |       expect(results![0]!.metadata.kind).toBe("vault");
      :              ^^^^^^^^^^^^
 1019 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1018:14]
 1017 |       expect(results!.length).toBe(1);
 1018 |       expect(results![0]!.metadata.kind).toBe("vault");
      :              ^^^^^^^^
 1019 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1019:14]
 1018 |       expect(results![0]!.metadata.kind).toBe("vault");
 1019 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
      :              ^^^^^^^^^^^^
 1020 |     });
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1019:14]
 1018 |       expect(results![0]!.metadata.kind).toBe("vault");
 1019 |       expect(results![0]!.metadata.sourcePath).toBe("vault/shared/context.md");
      :              ^^^^^^^^
 1020 |     });
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1077:14]
 1076 |       expect(results).not.toBeNull();
 1077 |       expect(results!.length).toBe(1);
      :              ^^^^^^^^
 1078 |       expect(results![0]!.metadata.kind).toBe("code");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1078:14]
 1077 |       expect(results!.length).toBe(1);
 1078 |       expect(results![0]!.metadata.kind).toBe("code");
      :              ^^^^^^^^^^^^
 1079 |       expect(results![0]!.metadata.sourcePath).toBe("src/runtime/runtime.ts");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1078:14]
 1077 |       expect(results!.length).toBe(1);
 1078 |       expect(results![0]!.metadata.kind).toBe("code");
      :              ^^^^^^^^
 1079 |       expect(results![0]!.metadata.sourcePath).toBe("src/runtime/runtime.ts");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1079:14]
 1078 |       expect(results![0]!.metadata.kind).toBe("code");
 1079 |       expect(results![0]!.metadata.sourcePath).toBe("src/runtime/runtime.ts");
      :              ^^^^^^^^^^^^
 1080 |     });
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1079:14]
 1078 |       expect(results![0]!.metadata.kind).toBe("code");
 1079 |       expect(results![0]!.metadata.sourcePath).toBe("src/runtime/runtime.ts");
      :              ^^^^^^^^
 1080 |     });
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1137:14]
 1136 |       expect(results).not.toBeNull();
 1137 |       expect(results!.length).toBe(2);
      :              ^^^^^^^^
 1138 |       const kinds = results!.map((r) => r.metadata.kind).sort();
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1138:21]
 1137 |       expect(results!.length).toBe(2);
 1138 |       const kinds = results!.map((r) => r.metadata.kind).sort();
      :                     ^^^^^^^^
 1139 |       expect(kinds).toEqual(["code", "vault"]);
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1432:11]
 1431 |     // Record events independently
 1432 |     await sessionRuntimes.get("session-aaa")!.recordEvent({
      :           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 1433 |       vendor: "opencode",
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1438:11]
 1437 |     });
 1438 |     await sessionRuntimes.get("session-bbb")!.recordEvent({
      :           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 1439 |       vendor: "opencode",
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1446:12]
 1445 |     // Each session holds its own plan
 1446 |     expect(sessionRuntimes.get("session-aaa")!.getStatus().planSlug).toBe("session-alpha");
      :            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 1447 |     expect(sessionRuntimes.get("session-bbb")!.getStatus().planSlug).toBe("session-beta");
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
      ,-[tests/runtime.test.ts:1447:12]
 1446 |     expect(sessionRuntimes.get("session-aaa")!.getStatus().planSlug).toBe("session-alpha");
 1447 |     expect(sessionRuntimes.get("session-bbb")!.getStatus().planSlug).toBe("session-beta");
      :            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 1448 | 
      `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint(no-unused-vars): Identifier 'isManagedPlanUnavailable' is imported but never used.
    ,-[tests/runtime.test.ts:10:70]
  9 | import { getRuntimePaths, getVaultPaths, readStatus, readVaultContext } from "../src/runtime/persistence.js";
 10 | import { HybridLoopRuntime, computeDecision, resolveManagedPlanPath, isManagedPlanUnavailable } from "../src/runtime/runtime.js";
    :                                                                      ^^^^^^^^^^^^|^^^^^^^^^^^
    :                                                                                  `-- 'isManagedPlanUnavailable' is imported here
 11 | import type { DecisionInput } from "../src/runtime/runtime.js";
    `----
  help: Consider removing this import.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
    ,-[tests/runtime.test.ts:32:36]
 31 | vi.mock("../src/runtime/vault-embeddings.js", () => ({
 32 |   embed: vi.fn().mockResolvedValue(new Array(384).fill(0)),
    :                                    ^^^^^^^^^^^^^^
 33 |   embedBatch: vi.fn().mockResolvedValue([]),
    `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
     ,-[tests/runtime.test.ts:807:42]
 806 |       vi.mocked(buildUnifiedIndex).mockResolvedValue(null);
 807 |       vi.mocked(embed).mockResolvedValue(new Array(384).fill(0));
     :                                          ^^^^^^^^^^^^^^
 808 |     });
     `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
     ,-[tests/runtime.test.ts:881:24]
 880 |       // Query vector aligns with item-1 (dim 0)
 881 |       const queryVec = new Array(EMBEDDING_DIM).fill(0);
     :                        ^^^^^^^^^^^^^^^^^^^^^^^^
 882 |       queryVec[0] = 1.0;
     `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
     ,-[tests/runtime.test.ts:945:24]
 944 |       // We give them both an identical query vector
 945 |       const queryVec = new Array(EMBEDDING_DIM).fill(0);
     :                        ^^^^^^^^^^^^^^^^^^^^^^^^
 946 |       queryVec[0] = 1.0;
     `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
      ,-[tests/runtime.test.ts:1007:24]
 1006 |       vi.mocked(buildUnifiedIndex).mockResolvedValueOnce({ index: mockIndex, vectors });
 1007 |       const queryVec = new Array(EMBEDDING_DIM).fill(0);
      :                        ^^^^^^^^^^^^^^^^^^^^^^^^
 1008 |       queryVec[0] = 1.0;
      `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
      ,-[tests/runtime.test.ts:1067:24]
 1066 |       vi.mocked(buildUnifiedIndex).mockResolvedValueOnce({ index: mockIndex, vectors });
 1067 |       const queryVec = new Array(EMBEDDING_DIM).fill(0);
      :                        ^^^^^^^^^^^^^^^^^^^^^^^^
 1068 |       queryVec[0] = 1.0;
      `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  x eslint-plugin-unicorn(no-new-array): Do not use `new Array(singleArgument)`.
      ,-[tests/runtime.test.ts:1127:24]
 1126 |       vi.mocked(buildUnifiedIndex).mockResolvedValueOnce({ index: mockIndex, vectors });
 1127 |       const queryVec = new Array(EMBEDDING_DIM).fill(0);
      :                        ^^^^^^^^^^^^^^^^^^^^^^^^
 1128 |       queryVec[0] = 1.0;
      `----
  help: It's not clear whether the argument is meant to be the length of the array or the only element. If the argument is the array's length, consider using `Array.from({ length: n })`. If the argument is the only element, use `[element]`.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:317:11]
 316 |     // Idle returns null when no plan binding (no runtime available for hf agent either)
 317 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 318 |     const idleResult = await sessionIdle({ sessionID: "session-no-plan" });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:355:11]
 354 | 
 355 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 356 |     const decision = await sessionIdle(
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:393:11]
 392 | 
 393 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 394 |     await sessionIdle({
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:463:11]
 462 | 
 463 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 464 |     const decision = await sessionIdle({
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:636:11]
 635 | 
 636 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 637 |     const decision = await sessionIdle({ sessionID: "enriched-idle" });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:682:11]
 681 | 
 682 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 683 |     const decision = await sessionIdle({ sessionID: "enriched-loop" });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:704:11]
 703 |     // message.updated uses context.session.id as fallback when info has no sessionID.
 704 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 705 |     await hooks["message.updated"]!({
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:705:11]
 704 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
 705 |     await hooks["message.updated"]!({
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 706 |       info: { role: "assistant", agent: "hf-builder", error: { name: "MessageAbortedError", data: { message: "User interrupted generation" } } }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:709:26]
 708 | 
 709 |     const result = await hooks["session.idle"]!({ sessionID: "interrupt-test" });
     :                          ^^^^^^^^^^^^^^^^^^^^^^
 710 |     expect(result).toMatchObject({
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:729:11]
 728 |     // Activate hf agent, trigger interrupt
 729 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 730 |     await hooks["message.updated"]!({
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:730:11]
 729 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
 730 |     await hooks["message.updated"]!({
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 731 |       info: { role: "assistant", agent: "hf-builder", error: { name: "MessageAbortedError", data: { message: "User interrupted generation" } } }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:735:25]
 734 |     // First idle consumes the interrupt
 735 |     const first = await hooks["session.idle"]!({ sessionID: "consume-test" });
     :                         ^^^^^^^^^^^^^^^^^^^^^^
 736 |     expect(first).toMatchObject({ action: "allow_stop" });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:739:26]
 738 |     // Second idle: interrupted is false, activeAgentIsHf is true, but no plan binding → null
 739 |     const second = await hooks["session.idle"]!({ sessionID: "consume-test" });
     :                          ^^^^^^^^^^^^^^^^^^^^^^
 740 |     expect(second).toBeNull();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:749:26]
 748 |     // No message.updated call — activeAgentIsHf defaults to false
 749 |     const result = await hooks["session.idle"]!({ sessionID: "no-agent-test" });
     :                          ^^^^^^^^^^^^^^^^^^^^^^
 750 |     expect(result).toBeNull();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:759:11]
 758 |     // Set a non-hf agent
 759 |     await hooks["message.updated"]!({ info: { role: "assistant", agent: "some-other-agent" } });
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 760 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:761:26]
 760 | 
 761 |     const result = await hooks["session.idle"]!({ sessionID: "non-hf-test" });
     :                          ^^^^^^^^^^^^^^^^^^^^^^
 762 |     expect(result).toBeNull();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:771:7]
 770 |     await expect(
 771 |       hooks["tool.execute.before"]!({ command: "rm -rf /" })
     :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 772 |     ).rejects.toThrow("Hybrid runtime guardrail blocked a destructive command.");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:775:11]
 774 |     // Set non-hf agent explicitly
 775 |     await hooks["message.updated"]!({ info: { role: "assistant", agent: "some-other-agent" } });
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 776 |     await expect(
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:777:7]
 776 |     await expect(
 777 |       hooks["tool.execute.before"]!({ command: "git reset --hard HEAD~1" })
     :       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 778 |     ).rejects.toThrow("Hybrid runtime guardrail blocked a destructive command.");
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:789:26]
 788 |     // No message.updated — activeAgentIsHf is false, but session.created is not gated
 789 |     const result = await hooks["session.created"]!({ sessionID: "created-test" }) as { additionalContext?: string };
     :                          ^^^^^^^^^^^^^^^^^^^^^^^^^
 790 |     expect(result).toBeDefined();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:809:11]
 808 |     // Trigger runtime hydration for both sessions
 809 |     await hooks["session.created"]!({ sessionID: "session-A" });
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 810 |     await hooks["session.created"]!({ sessionID: "session-B" });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:810:11]
 809 |     await hooks["session.created"]!({ sessionID: "session-A" });
 810 |     await hooks["session.created"]!({ sessionID: "session-B" });
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 811 | 
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:817:28]
 816 | 
 817 |     const runtimeA = await sessionRuntimes.get("session-A")!;
     :                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 818 |     const runtimeB = await sessionRuntimes.get("session-B")!;
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:818:28]
 817 |     const runtimeA = await sessionRuntimes.get("session-A")!;
 818 |     const runtimeB = await sessionRuntimes.get("session-B")!;
     :                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 819 |     expect(runtimeA).not.toBeNull();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:830:11]
 829 |     // Activate hf agent and interrupt for context.session.id (default fallback)
 830 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 831 |     await hooks["message.updated"]!({
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:831:11]
 830 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
 831 |     await hooks["message.updated"]!({
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 832 |       info: { role: "assistant", agent: "hf-builder", error: { name: "MessageAbortedError" } }
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:836:33]
 835 |     // session-flags-default: interrupted = true, activeAgentIsHf = true
 836 |     const resultDefault = await hooks["session.idle"]!({ sessionID: "session-flags-default" });
     :                                 ^^^^^^^^^^^^^^^^^^^^^^
 837 |     expect(resultDefault).toMatchObject({ action: "allow_stop", reason: expect.stringContaining("interrupted") });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:840:31]
 839 |     // session-other: has its own independent flags (defaults to false/false)
 840 |     const resultOther = await hooks["session.idle"]!({ sessionID: "session-other" });
     :                               ^^^^^^^^^^^^^^^^^^^^^^
 841 |     expect(resultOther).toBeNull(); // non-hf agent (default), not interrupted
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:853:18]
 852 |     // Hooks no-op gracefully
 853 |     await expect(hooks["session.created"]!({ sessionID: "unbound" })).resolves.toMatchObject({});
     :                  ^^^^^^^^^^^^^^^^^^^^^^^^^
 854 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:854:11]
 853 |     await expect(hooks["session.created"]!({ sessionID: "unbound" })).resolves.toMatchObject({});
 854 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
     :           ^^^^^^^^^^^^^^^^^^^^^^^^^
 855 |     await expect(hooks["session.idle"]!({ sessionID: "unbound" })).resolves.toBeNull();
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[tests/adapters.test.ts:855:18]
 854 |     await hooks["message.updated"]!(HF_AGENT_MESSAGE);
 855 |     await expect(hooks["session.idle"]!({ sessionID: "unbound" })).resolves.toBeNull();
     :                  ^^^^^^^^^^^^^^^^^^^^^^
 856 |   });
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  ! typescript-eslint(no-non-null-assertion): Forbidden non-null assertion.
     ,-[src/runtime/plan-doc.ts:130:20]
 129 |     if (milestones.length > 0 && pendingMetadata.length > 0) {
 130 |       const last = milestones[milestones.length - 1]!;
     :                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 131 |       const parsed = parseMetadataLines(pendingMetadata);
     `----
  help: Consider using the optional chain operator `?.` instead. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator.

  x eslint-plugin-unicorn(no-useless-fallback-in-spread): Empty fallbacks in spreads are unnecessary
     ,-[src/bin/hf-setup.ts:293:9]
 292 |       adapters: {
 293 |         ...(manifest.adapters ?? {})
     :         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 294 |       }
     `----
  help: Spreading falsy values in object literals won't add any unexpected properties, so it's unnecessary to add an empty object as fallback.

  x eslint-plugin-unicorn(no-useless-fallback-in-spread): Empty fallbacks in spreads are unnecessary
     ,-[src/bin/hf-setup.ts:322:9]
 321 |       adapters: {
 322 |         ...(manifest.adapters ?? {})
     :         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
 323 |       }
     `----
  help: Spreading falsy values in object literals won't add any unexpected properties, so it's unnecessary to add an empty object as fallback.

  x eslint(max-lines-per-function): The async function runSetup has too many lines (104). Maximum allowed is 50.
     ,-[src/bin/hf-setup.ts:234:1]
 233 |     
 234 | ,-> async function runSetup(command: ActionChoice, platform: PlatformChoice, opts: WizardOptions): Promise<void> {
 235 | |     // Dynamic imports of mjs lib files — resolved via file:// URL so Node ESM resolves correctly
 236 | |     const helpers = (await import(`file://${path.join(libDir, "install-helpers.mjs")}`)) as HelpersLib;
 237 | |     const claudeLib = (await import(`file://${path.join(libDir, "install-claude.mjs")}`)) as ClaudeLib;
 238 | |     const opencodeLib = (await import(`file://${path.join(libDir, "install-opencode.mjs")}`)) as OpencodeLib;
 239 | |   
 240 | |     // Map wizard platform choice to helpers tool value
 241 | |     const toolMap: Record<PlatformChoice, string> = {
 242 | |       both: "all",
 243 | |       claude: "claude",
 244 | |       opencode: "opencode"
 245 | |     };
 246 | |   
 247 | |     const cliBits: string[] = [command, "--tool", toolMap[platform]];
 248 | |     if (opts.targetDir !== null) {
 249 | |       cliBits.push("--target-dir", opts.targetDir);
 250 | |     }
 251 | |     if (opts.skipBuild) {
 252 | |       cliBits.push("--skip-build");
 253 | |     }
 254 | |     if (opts.configPath !== null) {
 255 | |       cliBits.push("--config", opts.configPath);
 256 | |     }
 257 | |   
 258 | |     const options = helpers.parseArgs(cliBits);
 259 | |   
 260 | |     helpers.ensureTargetDir(options.targetDir);
 261 | |   
 262 | |     const packageName = helpers.getPackageName();
 263 | |     const config = helpers.loadProjectConfig(options);
 264 | |     const manifest = helpers.readManifest(options.targetDir);
 265 | |     const adapters = helpers.resolveAdapterSelection(options, config.value, manifest);
 266 | |     const scaffold = helpers.resolveScaffoldSelection(options, config.value);
 267 | |   
 268 | |     const shouldScaffold = command === "init" && (scaffold.plans || scaffold.vault);
 269 | |   
 270 | |     if (!adapters.claude && !adapters.opencode && command !== "uninstall" && !shouldScaffold) {
 271 | |       helpers.log(`No adapters are enabled for ${command}.`);
 272 | |       if (config.exists) {
 273 | |         helpers.log(`Config checked: ${path.relative(options.targetDir, config.path)}`);
 274 | |       }
 275 | |       return;
 276 | |     }
 277 | |   
 278 | |     if (
 279 | |       (command === "install" || command === "init") &&
 280 | |       !options.skipBuild &&
 281 | |       !helpers.isInstalledPackage() &&
 282 | |       (adapters.claude || adapters.opencode)
 283 | |     ) {
 284 | |       helpers.run("npm", ["install"]);
 285 | |       helpers.run("npm", ["run", "build"]);
 286 | |     }
 287 | |   
 288 | |     function executeInstall(commandLabel: string): void {
 289 | |       const nextManifest: Manifest = {
 290 | |         version: 1,
 291 | |         packageName,
 292 | |         adapters: {
 293 | |           ...(manifest.adapters ?? {})
 294 | |         }
 295 | |       };
 296 | |   
 297 | |       opencodeLib.runAssetSync(options, adapters);
 298 | |   
 299 | |       if (adapters.claude) {
 300 | |         nextManifest.adapters["claude"] = claudeLib.installClaude(
 301 | |           options.platform,
 302 | |           options.targetDir,
 303 | |           packageName
 304 | |         );
 305 | |       }
 306 | |   
 307 | |       if (adapters.opencode) {
 308 | |         nextManifest.adapters["opencode"] = opencodeLib.installOpenCode(
 309 | |           options.targetDir,
 310 | |           packageName
 311 | |         );
 312 | |       }
 313 | |   
 314 | |       helpers.writeManifest(options.targetDir, nextManifest);
 315 | |       helpers.log(`Hybrid runtime ${commandLabel} complete.`);
 316 | |     }
 317 | |   
 318 | |     function executeUninstall(): void {
 319 | |       const nextManifest: Manifest = {
 320 | |         ...manifest,
 321 | |         adapters: {
 322 | |           ...(manifest.adapters ?? {})
 323 | |         }
 324 | |       };
 325 | |   
 326 | |       if (adapters.claude) {
 327 | |         claudeLib.uninstallClaude(options.targetDir, manifest.adapters["claude"]);
 328 | |         delete nextManifest.adapters["claude"];
 329 | |       }
 330 | |   
 331 | |       if (adapters.opencode) {
 332 | |         opencodeLib.uninstallOpenCode(options.targetDir, manifest.adapters["opencode"]);
 333 | |         delete nextManifest.adapters["opencode"];
 334 | |       }
 335 | |   
 336 | |       helpers.writeManifest(options.targetDir, nextManifest);
 337 | |       helpers.log("Hybrid runtime uninstall complete.");
 338 | |     }
 339 | |   
 340 | |     if (command === "install") {
 341 | |       executeInstall("install");
 342 | |       return;
 343 | |     }
 344 | |   
 345 | |     if (command === "sync") {
 346 | |       executeInstall("sync");
 347 | |       return;
 348 | |     }
 349 | |   
 350 | |     if (command === "uninstall") {
 351 | |       executeUninstall();
 352 | |       return;
 353 | |     }
 354 | |   
 355 | |     if (command === "init") {
 356 | |       helpers.seedIndexConfig(config);
 357 | |       helpers.scaffoldProject(options.targetDir, scaffold);
 358 | |       executeInstall("install");
 359 | |     }
 360 | `-> }
 361 |     
     `----
  help: Consider splitting it into smaller functions.

Found 227 warnings and 51 errors.
Finished in 246ms on 49 files with 95 rules using 16 threads.
